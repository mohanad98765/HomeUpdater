# سجل التغييرات (CHANGELOG)

جميع التغييرات المهمّة في مشروع **HomeUpdater / محدِّث المنزل** تُوثَّق هنا.

التنسيق مبنيّ على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)،
والمشروع يتبع [الإصدار الدلالي (SemVer)](https://semver.org/lang/ar/).

## [1.22.0] — 2026-08-09

### أُصلح (Fixed) — لماذا لا تظهر بعض الأجهزة في فحص الشبكة

جرّب زميل النسخة على شبكة عمله فلم تظهر عنده أجهزة يعرف أنّها موجودة. **والقياس
أوّلًا: على شبكة المكتب هنا لا يُخطئ الماسح إطلاقًا** — تسعة عشر جهازًا حقيقيًّا حسب
ويندوز نفسه، والماسح وجد **١٩ من ١٩** في ٦٫٧ ثانية، ثلاث مرّات متطابقة. فالخلل ليس
في الفحص عمومًا، بل في شروطٍ تتحقّق في شبكة شركة ولا تتحقّق في بيت.

وأُصلحت ستّة أعطال، كلّها نُفِّذت على الكود المشحون لا قُرئت فيه:

**١) الشبكة الأوسع كانت تنكمش صامتة.** أي شبكة أوسع من ١٠٢٤ عنوانًا كان يُفحص منها
٢٥٤ فقط. مقيسًا: على /21 لا يُجسّ **١٧٩٢ من ٢٠٤٦ عنوانًا (٨٧٫٦٪)**، وعلى /16
**٩٩٫٦٪** — والنتيجة تُعرض كنجاح.

**٢) توسيع النطاق يدويًّا كان لا يفعل شيئًا.** الحدّ كان يتجاهل النطاق المطلوب ويعيد
اشتقاق /24 حول عنوان الجهاز نفسه، فطلبُ `10.20.0.0/16` من جهاز على 192.168.3.x كان
يفحص 192.168.3.0/24 — **صفر** من العناوين المطلوبة. صار المسح **لا يخرج أبدًا من
الشبكة المطلوبة**.

**٣) التغطية صارت تُقال لا تُخفى.** فحصٌ غطّى ٢٥٤ من ٢٠٤٦ وشبكةٌ فيها ٢٥٤ جهازًا
يعطيان الشاشة نفسها. صار شريط ثابت فوق النتيجة يقول كم عنوانًا فُحص وكم تُخطّي —
ويبقى ظاهرًا بعد إعادة فتح الصفحة، لا سطرًا في سجلّ يمرّ.

**٤) عنوان النفق (VPN) يُرفض برسالة بدل فحص عنوان واحد.** قناع /32 كان يمرّ من فحص
الحجم (٢-١ = ‎-١) فيُفحص عنوان واحد — الجهاز نفسه — ويُبلَّغ بالنجاح بلا تحذير.

**٥) الأجهزة خلف الموجّه لم تعد تنطوي في صفّ واحد.** الموجّه يردّ على ARP نيابةً عن
كلّ ما يُوجّه إليه، فكانت هوية الجهاز المبنيّة على MAC تجمع شبكة فرعية كاملة في صفّ
واحد. مقيسًا على هذه الشبكة: `192.168.3.1` و`192.168.100.5` لهما نفس العنوان الفيزيائيّ،
وقاعدة البيانات كانت تحوي صفًّا واحدًا لهما. الآن يملك الموجّه عنوانه، وكلّ جهاز خلفه
صفّه.

**٦) مصافحة TCP صارت دليل حياة.** كان وجود الجهاز يقرّره جدول ARP وحده، والمصافحة
الناجحة تُرمى — فأي شيء يمنع قيد ARP كان يعني اختفاءً لا نقصًا في المعلومة. وأُصلح
معه حذف العناوين المنتهية بـ‏.255 (صحيح على /24 وحده: على /21 يحذف سبعة أجهزة
حقيقية)، وتصفية البثّ المتعدّد صارت بالمدى لا بأوّل ثُماني.

### أُضيف (Added)
[network_diagnostic.ps1](02_التطوير/scripts/network_diagnostic.ps1) — سكربت قراءة فقط
يجمع ما لا يُعرف إلّا من جهاز المستخدم: أي بطاقة تملك المسار، وعرض الشبكة الحقيقيّ،
ووجود VPN، وكم جارًا يرى ويندوز نفسه — ويكتب تقريرًا واحدًا يُرسَل.

‎+١٠ اختبارات خلفيّة و+٢ واجهة، **٦١٦ خلفيّة و١٨٨ واجهة**. واختبارٌ كان يُثبّت السلوك
الخاطئ (انطواء الأجهزة على MAC واحد) أُعيدت كتابته ليُثبّت الصحيح.

## [1.21.0] — 2026-08-09

### أُضيف (Added) — تصوير الرمز يكفي: البرنامج يُقرن نفسه

طلب المدير أن **يسير الاتّصال تلقائيًّا فور تصوير الرمز والموافقة على الهاتف**.
وهذا ما كان ينقص: الرمز كان يُعرَض، والهاتف يُصوِّره ويوافق، ثمّ **ينتظر الطرفان
إعلانًا لا تنقله الشبكة** — فتنتهي المدّة.

والمفتاح أنّ **كلمة المرور في الرمز كلمتنا نحن**. فإن عرفنا المنفذ، أقرنّا بأنفسنا
بلا حرف واحد من المستخدم. والهاتف يفتح ذلك المنفذ **بعد** المسح مباشرةً.

فصارت الجلسة تعمل بالطريقين معًا:

- **مسح المنافذ** على الهاتف الذي اختاره المستخدم من القائمة — يعمل على أي شبكة
- **الإعلان (mDNS)** كما كان — يعمل على الشبكات التي تنقله

وتُلتقط **صورة المنافذ المفتوحة قبل عرض الرمز**، فالمنفذ الذي كان مفتوحًا أصلًا
لا يُخلط أبدًا بالمنفذ الذي فتحه المسح، ولا تُسلَّم كلمة المرور لخدمة لا علاقة
لها بنا. ولا يُمسح إلّا الجهاز الذي اختاره المستخدم — لا الشبكة كلّها.

### قِيس (Measured) — من ١٦ ثانية إلى ٦٫٢

سرعة المسحة هي الفرق بين «تلقائيّ» و«انتظر». قِيست على الهاتف الحقيقيّ،
٢٠ ألف منفذ في كلّ مرّة:

| التوازي | المهلة | الثواني | مسحات في دقيقتين |
|---:|---:|---:|---:|
| ٨٠٠ | ٠٫٦ | ١٦٫٠ | ٧٫٥ |
| ١٥٠٠ | ٠٫٤ | ٦٫٧ | ١٨٫٠ |
| **٢٥٠٠** | **٠٫٣** | **٦٫٢** | **١٩٫٢** |
| ٤٠٠٠ | ٠٫٢٥ | ٦٫٩ | ١٧٫٣ |

الدفع إلى ٤٠٠٠ **أبطأ** لا أسرع. فاعتُمد ٢٥٠٠/٠٫٣: تسع عشرة مسحة داخل نافذة
الدقيقتين، أي أنّ المنفذ يُوجَد بعد لحظات من المسح لا في آخر انتظار طويل.

### حُسّن (Changed)
اختيار الهاتف صار **مُستحسَنًا ومشروحًا** في شاشة QR، والسطر تحته يقول أي
الطريقين يعمل الآن: «يُضاف الجهاز تلقائيًّا» أو «سينتظر إعلان الهاتف — وكثير
من الشبكات لا تنقله».

‎+٤ اختبارات خلفيّة و+٤ واجهة. المجموع **٦٠٣ خلفيّة و١٨٦ واجهة**.

## [1.20.1] — 2026-08-09

### حُسّن (Changed) — الطريق المسدود صار إشارة
جرّب زميل البرنامج على **شبكة مكان عمله** ففشل إقران QR عنده أيضًا — وهذا
**يدعم التشخيص لا ينقضه**: شبكات الشركات أكثر مكان يُحجب فيه البثّ المتعدّد (عزل
العملاء وشبكات VLAN منفصلة إعداد افتراضيّ).

والدرس ليس في الشبكة بل في المنتج: الجلسة كانت تنتهي بكلمة «انتهت المدّة»
وقائمة أربعة احتمالات — فصارت ستّ محاولات ستّة طرق مسدودة. **والعلاج على
بُعد لوحة واحدة منها.**

صارت الجلسة المنتهية تقول السبب الأرجح صراحةً — «شبكتك لا تنقل إعلان الهاتف» —
وتشير إلى «إقران بالرمز» الذي لا يحتاجه ويعمل على أي شبكة. ولا تتّهم الشبكة
حين لا يكون لدى المركز سبب لذلك — واختبار يحرس الأمرين.

‎+٢ اختبارًا. المجموع **٥٩٩ خلفيّة و١٨٢ واجهة**.

## [1.20.0] — 2026-08-09

### أُضيف (Added) — إقران برمز سداسيّ يعمل على أي شبكة
ستّ محاولات إقران QR فشلت، والسبب لم يكن في التطبيق إطلاقًا:

- الهاتف يستجيب للping في **٤٠ م.ث**
- ويرسل **صفر حزمة mDNS في ٦٠ ثانية**، بينما تصل خمس من مضيفين آخرين
- الحاسب **سلكيّ** والهاتف **لاسلكيّ**، والراوتر لا يمرّر البثّ المتعدّد بينهما

وآليّة أندرويد كلّها مبنيّة على أن يسمع الحاسب إعلان الهاتف عن منفذ يختاره عشوائيًّا.

**والردّ ليس تعليم المستخدم ما هو mDNS.** من يشتري هذا المنتج يدير محلًّا أو
مكتبًا منزليًّا؛ ومطالبته بتفعيل IGMP في راوتره أو قراءة منفذ عشوائيّ من شاشة هاتف
هو مطالبته بأن يصير مهندس شبكات ليُضيف هاتفًا. **ذلك فشل منتج لا فشل مستخدم.**

فصار المركز **يبحث عن المنفذ بدل أن يسمعه**: منفذ TCP على مضيف يعرف عنوانه،
والاتّصال المباشر يعمل. **مقيس على الهاتف الحقيقيّ: ٢٠ألف منفذ في ١٦ ثانية**،
بلا إنذارات كاذبة حين لا يكون شيء مفتوحًا.

**عمل المستخدم الآن:** يختار هاتفه **من قائمة** (التطبيق يعرف أجهزة الشبكة
بأسمائها ومصنّعيها من فحصه الخاصّ)، ثم يكتب **ستّة أرقام**. لا عنوان،
ولا منفذ، ولا إعدادات راوتر. والهاتف يُضاف إلى القائمة وحده.

والرسائل مكتوبة لإنسان لا لمطوّر: إن لم يُعثَر على منفذ تقول الرسالة إنّ شاشة
الإقران أُغلقت (وإغلاقها يُغلق المنفذ)، ورمزٌ خاطئ **لا يُعاد على كلّ منفذ**
لأنّ ذلك فشل مؤكّد يحرق وقت من يقف أمام هاتفه.

‎+٢٢ اختبارًا (١٣ خلفيّة و٩ واجهة)، وأوّلها يحرس القيد الحاكم: **لا حقل نصّيّ
واحد في اللوحة إلّا الرمز**. المجموع **٥٩٩ خلفيّة و١٨٠ واجهة**.

وإقران QR باقٍ كخيار ثانٍ — يعمل حيث تمرّر الشبكة البثّ المتعدّد.

## [1.19.0] — 2026-08-09

### حُسِّن (Changed) — المسح يُضيف الجهاز، لا يملأ حقلًا
استعملها مهنّد فقال: «أبغى لمّا أصوّر الكود ينضاف الجهاز في البرنامج». وكان محقًّا:
الإقران الناجح كان **يملأ العنوان والمنفذ وينتظر ضغطة ثانية** على «إضافة هاتف».
ومَن مسح رمزًا فقد قال أي هاتف يقصد؛ ونموذجٌ نصفُ ممتلئ يُقرأ على أنّه «ما اشتغل» —
وهذا بالضبط ما حدث في الاستعمال. صار الإقران الناجح **يُسجّل الجهاز بنفسه**، مع سطر
«يُضاف الجهاز…» ورسالة صريحة إن نجح الإقران وفشلت الإضافة (وهما حالتان مختلفتان).

### أُضيف (Added) — تحذير يسبق الخطوات: **الكاميرا العاديّة لن تعمل**
خمس جلسات إقران انتهت دون استعمال والرمز يُعرض سليمًا. والسبب في الحمولة نفسها:

```
WIFI:T:ADB;S:homeupdater-…;P:……;;
```

تبدأ بالبادئة الحرفيّة `WIFI:` — وهي صيغة **الانضمام إلى شبكة واي فاي** القياسيّة.
فتطبيق الكاميرا أو أي قارئ QR يراها ويعرض «الانضمام إلى شبكة»، ولا يعرف ما `T:ADB`،
فلا يصل إلى adb أبدًا. **مكانٌ واحد على الهاتف يفهمه**: شاشة «إقران جهاز برمز QR»
داخل «تصحيح الأخطاء اللاسلكي».

كان هذا مذكورًا — كخطوة ٢ من ٣ بخطٍّ رماديّ صغير. صار **لافتة تحذير فوق الخطوات**،
لأنّه الفرق الحقيقيّ بين أن تعمل الميزة وألّا تعمل. وفي اللغات الستّ.

‎+٢ اختبارًا: أحدهما يؤكّد أن الجهاز يُسجَّل **بلا نقرة ثانية** وبالعنوان والمنفذ
الصحيحين، والآخر يؤكّد أن التحذير موجود **وقبل** الخطوات المرقّمة — لا وجوده وحده.
المجموع **٥٨٦ خلفيّة و١٧١ واجهة**.

## [1.18.4] — 2026-08-09

### أُصلح (Security) — cryptography 48.0.1 ← 50.0.0
ثلاث ثغرات جديدة (PYSEC-2026-3552/3553/3554) أوقفت بوّابة الأمان. وهذه المكتبة
تحمل **توقيع الوكلاء Ed25519، وشهادة TLS للمركز، وتشفير بيانات الاعتماد
عند التخزين** — أي أنّ تأجيلها ليس خيارًا. وتحقّقتُ أن asyncssh 2.18.0 يقبلها،
وأنّ توقيع Ed25519 والتحقّق منه يعملان.

### أُصلح (Fixed) — اختبار لا يستطيع الفشل إلّا على جهاز المطوّر
سقط `test_expired_key_is_valid_but_not_entitled` ولم يكن لـcryptography علاقة — يسقط
بنفس الطريقة على 48.0.1. الاختبار يختم الصلاحيّة بـ`date.today()` **المحلّي**،
والفاحص يقارن بـ`datetime.now(UTC).date()`. ففي توقيت UTC+3 يختلف التاريخان
ثلاث ساعات كل ليلة، **وCI يعمل بتوقيت UTC فلا يراه أبدًا**.

والأهمّ أنّه كشف غموضًا في المنتج: `tools/make_license.py` يختم `expires` بتاريخ
**محلّي** و`issued_at` بـ**UTC** — في الدالّة نفسها. فمفتاحٌ يقول «صالح حتّى ٣١
ديسمبر» مُصدَر من الرياض يموت فعليًّا الساعة ٣ فجرًا من ١ يناير. صار الثلاثة
يستعملون UTC.

المجموع **٥٨٦ خلفيّة و١٦٩ واجهة**.

## [1.18.3] — 2026-07-27

### قِياس (2026-08-09) — تصحيحٌ لسببٍ ادّعيتُه في 1.18.2/1.18.3
غيّرتُ ثلاثة أشياء في الرمز بناءً على المعقول لا على المقيس. قِستُها بـzxing-cpp —
وهي سلالة ماسح أندرويد نفسه — على حمولتنا نحن، والحقيقة معروفة مسبقًا:

| المرشّح | بكسل/وحدة | أصغر التقاط يُفكّ عنده (حادّ) | ضبابيّة ٢٫٥ |
|---|---|---|---|
| Q، ٢٣٠ بكسل (قبل 1.18.2) | ٥٫٦١ | **٦٠ بكسل** | ٢٠٥ |
| Q، ٣٠٠ بكسل (1.18.2) | ٧٫٣٢ | ٦٠ بكسل | ٢٠٥ |
| L، ٢٩٦ بكسل (الحاليّ) | ٨٫٠٠ | ٦٠ بكسل | **١٨٥** |

**الرمز الذي اتّهمتُه بالصِّغَر يُفكّ من التقاطٍ عرضه ٦٠ بكسلًا.** وهاتف يؤطّره على
٢٥ سم يعطي المستشعر مئات البكسلات. **فالحجم لم يكن السبب**، والإعداد الحاليّ تحسينٌ
حقيقيّ لكنّه ٧–١٠٪ في هامش الضبابيّة فقط — يبقى لأنّه مجّانيّ، لا لأنّه أصلح شيئًا.

والدحض الذي يهمّ: L يصحّح ٧٪ وQ يصحّح ٢٥٪، فيُفترض أن يخسر L عند التشويش. **لم يخسر**
— كلاهما يسقط عند ١٪ رشّ، لأن الرشّ بهذه الكثافة يُتلف أخذ عيّنات الوحدات لا بضع
كلمات ترميز. فالصفقة آمنة.

والحمولة **تعود حرفيًّا** من المُفكِّك: الرمز صحيح والنصّ صحيح.

القياس محفوظ وقابل للإعادة في `02_التطوير/backend/tools/qr_readability_bench.py`.
وzxing-cpp أداة قياس **لا اعتماديّة مُنتَج** — غائبة عن `requirements.txt` وعن
`HomeUpdater.spec`، ومُتحقَّق من ذلك.

**فسبب فشل الإقران الخمس مرّات ما زال مجهولًا** — والحجم لم يعد مرشّحًا.

### حُسِّن (Changed) — الرمز صار أسهل على الكاميرا، لا أكبر فحسب
تكبير الصندوق في 1.18.2 عالج نصف المسألة. والرقم الذي يقرّر فعلًا هو **البكسلات لكل
وحدة**، وهو يعتمد على عدد الوحدات — وعددُها يعتمد على مستوى تصحيح الخطأ. القياس:

| المستوى | الوحدات (بالإطار) | بكسل/وحدة عند ٣٠٠ |
|---|---|---|
| **L** | ٣٧ | **٨٫١١** |
| Q (السابق) | ٤١ | ٧٫٣٢ |
| H | ٤٥ | ٦٫٦٧ |

- صار المستوى **L**: تصحيح الخطأ يشتري تعافيًا من التلف والحجب، ورمزٌ على شاشة نظيفة
  لعشرين ثانية لا يعاني أيًّا منهما — فدفعُ ١١٪ من حجم الوحدة مقابل تعافٍ لن يُستعمل
  صفقةٌ خاسرة.
- **وعيب لم أنتبه له**: SVG الطبيعيّ ٢٩٦ بكسل وكنت أمطّه بـCSS إلى ٣٠٠ — نسبة ١٫٠١٣،
  فتقع كل حافّة وحدة في منتصف بكسل ويُنعّمها المُصيّر رماديًّا. **وهذا بالضبط ما يُربك
  مُميِّز الأبيض والأسود.** صار يُعرَض بحجمه الطبيعيّ ١:١ (٣٧ وحدة × ٨ = ٢٩٦).
- و`shape-rendering="crispEdges"` — لا يضعه segno — لأن العرض المكبَّر يقيس بنسبة من
  النافذة ولا يمكن أن يكون مضاعفًا صحيحًا، فبدونه تلين الحوافّ في اللحظة نفسها التي
  تكون فيها الكاميرا أمام الشاشة.
- أسود خالص على أبيض خالص: التباين هو النصف الآخر ممّا يحتاجه المُميِّز.

‎+٣ اختبارات تحرس الأرقام نفسها: المضاعف الصحيح، وحدٌّ أدنى ٦ بكسلات لكل وحدة،
و`crispEdges`، والتباين. المجموع **٥٨٦ خلفيّة و١٦٩ واجهة**.

**ولا يزال غير مُثبت**: مسحة ناجحة واحدة. سجلّ ٢٦ يوليو يُظهر **خمس جلسات إقران
وصفر نجاح** — كلّها على الرمز الصغير. هذه التحسينات فرضيّة مُعالَجة لا سببٌ مُثبت.

## [1.18.2] — 2026-07-26

### أُصلح (Fixed) — الرمز كان أصغر من أن تقرأه كاميرا هاتف
ثلاث محاولات إقران فاشلة، وبدت كلّها مشكلةَ شبكة. فأصغيتُ إلى mDNS بمقبس خام بمعزل
عن adb، وكانت النتيجة قاطعة:

- mDNS **يصل**: ١٩ حزمة من ٧ مضيفين في ٢٠ ثانية، والانضمام إلى `224.0.0.251` نجح.
- جدار الحماية **لا يمنع**.
- الهاتف على الشبكة **ويتكلّم mDNS** فعلًا.
- وخدمة `_adb-tls-pairing` من أي جهاز: **صفر**.

وأندرويد لا يُعلن خدمة الإقران إلّا **بعد** مسح رمز صحيح بنجاح. فالمسح لم يكتمل:
الرمز كان محدودًا بـ٢٣٠ بكسل، أي نحو **خمسة بكسلات لكل وحدة** في رمز من ٤١ وحدة —
كاميرا هاتف بزاوية أمام شاشة لامعة لا تقرأ ذلك.

- صار الرمز **٣٠٠ بكسل** افتراضيًّا، **وقابلًا للتكبير** إلى `min(78vw, 78vh)` بضغطة
  على الرمز نفسه أو على زرّ «كبِّر الرمز» — والعدّاد يبقى ظاهرًا وهو مكبَّر.
- وإغلاق العرض المكبَّر **لا يُنهي الجلسة**، فالرمز يبقى صالحًا.

### وتصحيحان لأخطاء ارتكبتُها في التشخيص
1. قلت إن v1.18.0/v1.18.1 «لم يُقلعا قطّ» استنادًا إلى سجلّ لا سطر فيه بعد 12:48.
   **غير صحيح** — يُقلعان سليمًا؛ التطبيق كان مُغلقًا فحسب لأن المثبِّت يُغلقه.
2. أوّل نسخة من مِصغاة mDNS أعلنت «الهاتف يُعلن فعلًا» — والحزم الثماني التي عدّتها
   كانت **استفساراتي أنا** ترتدّ من عنواننا. صار السكربت يستثني عنوان المضيف.

وعيب في طريقتي لا في المنتج: راصدٌ لا يعرف أن التطبيق مُغلق أحرق ٣٠٠ ثانية على لا شيء.
صار يرفض العمل قبل أن يتأكّد أن الخلفيّة تُجيب.

‎+٢ اختبارًا يحرسان القياس نفسه. المجموع **٥٨٣ خلفيّة و١٦٩ واجهة**.

## [1.18.1] — 2026-07-26

### أُصلح (Fixed) — **رمز QR لم يكن يظهر أبدًا**
أوّل تشغيل حقيقيّ: الجلسة بدأت والعدّاد يعمل، لكن مكان الرمز أيقونةُ صورة مكسورة.
السبب مقاس لا مُخمَّن:

```
GET /api/android/pair/qr.svg  ->  401  Missing or invalid session token
```

كل مسار تحت `/api/` يحتاج رمز الجلسة لكل إطلاق **ورمز الدخول**، ووسم `<img src>`
لا يرسل أيًّا منهما — مُحمِّل الصور في المتصفّح لا يحمل رؤوسًا مخصَّصة. فالصورة
**لا يمكن أن تُحمَّل**، لا عندي ولا عند أي عميل.

صار الرمز يُجلَب عبر `apiFetchText` (التي ترسل الرؤوس) ويُدرَج في الصفحة مباشرةً.
وإدراجه آمن: segno يرسمه من حمولة يبنيها التطبيق من أبجديّة ثابتة، فلا مدخل خارجيّ
يصل إلى ذلك الوسم — واختبارٌ يؤكّد أن الناتج بلا `<script>`. ويُمحى الرمز من الشاشة
عند الإلغاء وعند النجاح، فلا يبقى رمزٌ مستعمَل يدعو إلى مسحة ثانية.

### والأسوأ: **اختباري كان يُثبِّت العيب**
كان الاختبار يؤكّد أن الطلب **لا** يحدث:

```
expect(calls.some((c) => c === "GET /api/android/pair/qr.svg")).toBe(false);
// jsdom does not fetch <img src>, so assert the element points at the endpoint
```

لاحظتُ القيد وشحنتُ على أساسه. اختبارٌ يتساهل مع «الطلب لم يحدث» لا يرى الفرق بين
«jsdom لا يجلب الصور» و«الخادم يرفض الطلب». صار الاختبار **يطالب** بالجلب المُصادَق
ويرفض أي `<img>` يشير إلى مسار API. **وتحقّقت أنّه يمسك العيب**: أعدتُ العيب حرفيًّا
فسقط اختباران، ثم أرجعت الإصلاح.

‎+٢ اختبارًا. المجموع **٥٨٣ خلفيّة و١٦٧ واجهة**.

## [1.18.0] — 2026-07-26

### أُضيف (Added) — إقران الهاتف برمز QR
المسار اليدويّ يطلب من إنسان أن ينقل ثلاثة أشياء من شاشة هاتف إلى نافذة على جهاز
آخر: عنوانًا، ومنفذ إقران **يتغيّر كل مرّة**، ورمزًا ينتهي. صار المركز يعرض رمزًا
ويمسحه الهاتف.

- الحمولة `WIFI:T:ADB;S:<الاسم>;P:<كلمة المرور>;;` — وهي صيغة AOSP نفسها، مؤكَّدة
  من عدّة تنفيذات مستقلّة. وكلا الحقلين يُولَّد من أبجديّة ثابتة، فلا شيء يحتاج
  تهريبًا ولا مدخل خارجيّ يصل إلى هذا النصّ.
- **يُرسَم في الخلفيّة** بـsegno (بايثون خالص، بلا امتدادات مُصرَّفة): كلمة المرور
  تُولَّد هناك، فتظهر في **استجابة واحدة** فقط، وحزمة الـJS لا تدفع بايتًا واحدًا.
  والصورة تُقدَّم بـ`no-store` — فهي نفسها بيانات الاعتماد، ونسخة في مخزن WebView2
  تعيش أطول من الدقيقتين التي تنتمي إليها.
- **جلسة واحدة في وقت واحد**، ومدّتها دقيقتان، وكلمة المرور تُمحى عند الإلغاء وعند
  النجاح وعند إغلاق النافذة — رمزٌ حيّ متروك هو رمزٌ يستطيع غيرك استعماله.

### الفرضيّة التي رفضتُ أن أبني عليها
لم أستطع التحقّق — من أي مصدر — ممّا إذا كان الهاتف الذي مسح رمزنا يُعلن **الاسم
الذي وضعناه** في الرمز أم مُعرِّفه الخاصّ. والالتقاط الحقيقيّ الوحيد في المستودع
(من هاتف على هذه الشبكة) يُظهر مُعرِّفه الخاصّ `adb-<serial>-<random>`.

فالمطابقة **لا تعتمد على الاسم**: يُلتقط ما يُعلن قبل عرض الرمز، وكل ما يظهر **بعده**
مرشَّح. وإن حمل الاسم اسمَنا فهو إشارة تُرجَّح — لا شرطًا. واختبارٌ لكل الحالتين.
فرضيّةٌ غير مُتحقَّق منها لا يجوز أن تكون حاملة (وهذا درس v1.13.1).

### وما ترفض الصفحة أن تُوهم به
- **لا تُفعِّل «خيارات المطوّر» ولا «التصحيح اللاسلكي» نيابةً عنك** — تقول الطريق
  إليهما صراحةً، لأنّها لا تستطيع.
- **وعند الفشل لا تخمّن السبب.** `adb mdns check` مخرَجه نصّ ثابت في الملفّ التنفيذيّ
  وينجح دائمًا، فجدارُ حماية على شبكة «عامّة»، وهاتفٌ على شبكة أخرى، وشاشةٌ لم تُفتَح
  أصلًا — كلّها تبدو سواءً من هنا. فتُعرَض الأسباب الأربعة، ويُقال إن المركز لا يفرّق
  بينها لأن الهاتف هو مَن يبدأ الاتّصال.
- **وعند وصول هاتفين معًا يُسأل المشغّل** — الإقران بقرعة يسلّم كلمة المرور لهاتف
  لم يقصده.

‎+٢٩ اختبارًا (١٩ خلفيّة و١٠ واجهة). المجموع **٥٨٣ خلفيّة و١٦٥ واجهة**.

وما لم يُثبَت بعد: **مسحة واحدة بهاتف حقيقيّ**. كل ما قبلها حقيقيّ (adb وmDNS والرمز
والعدّاد)، أمّا المسح نفسه فيحتاج يدًا وهاتفًا.

## [1.17.1] — 2026-07-26

### أُصلح (Fixed) — **الوكيل لم يكن يستطيع الوصول إلى مركز TLS إطلاقًا**
شغّلتُ الرحلة كاملة لأوّل مرّة على مستمع TLS حقيقيّ ففشلت في أوّل خطوة: الوكيل يحسب
بصمة الشهادة ويقارنها ثمّ يفتح عميلًا عاديًّا — والعميل العاديّ يتحقّق من مخزن ثقة
النظام، فيرفض الشهادة الموقّعة ذاتيًّا بـ`CERTIFICATE_VERIFY_FAILED` **قبل أن يُستشار
التثبيت أصلًا**. وكل الاختبارات كانت تمرّ لأنّها تستعمل loopback‏ http، فما مرّ مسار
TLS ولا مرّة — ثلاثة إصدارات تقول «الوكيل يثبّت شهادة المركز» والتثبيت لم يعمل قطّ.

- صار **التثبيت داخل مصافحة TLS نفسها** لا فحصًا إلى جانبها: الشهادة المُثبَّتة هي
  مرساة الثقة الوحيدة — فيبقى التحقّق من الاسم (SAN) ومن الصلاحيّة، وهما ما كان
  `verify=False` مع مقارنة يدويّة سيُسقِطهما. ولا تبقى فرجة بين الفحص والاستعمال.
- وتُحفَظ الشهادة في حالة الوكيل (وليست سرًّا — المركز يسلّمها لكل من يتّصل)، وحالةٌ
  قديمة بلا شهادة لا تُعطّل الوكيل: البصمة المحفوظة تُثبت أي شهادة يُعيد جلبها.

### أُصلح (Fixed) — منفذ هاتف يُقرَأ من سطر هاتفٍ آخر (عيب مشحون)
تعبير تحليل `adb mdns services` كان يستعمل `\s+` وهو **يعبر الأسطر**، فصفٌّ خالي
العنوان يرتبط بعنوان السطر التالي — أي أن التطبيق قد يُبلِّغ بمنفذ هاتفٍ لهاتفٍ آخر.
صار التحليل سطرًا سطرًا ومُبنَيًّا (اسم الخدمة + النوع + العنوان + المنفذ)، وصار يقرأ
عناوين IPv6 بدل أن يُسقِطها بصمت.

### أُصلح (Fixed) — عنوان يُعرَض ولا ينفع
قائمة عناوين المركز كانت تعرض `fe80::…` وهو بلا معنى دون مُعرّف نطاق، والقائمة
يكتبها إنسان على جهاز آخر. عنوانٌ معروض لا يمكن أن يعمل أسوأ من عنوانٍ أقلّ.

### ما أُثبِت حيًّا (لا باختبار)
عمليّتان منفصلتان ومجلّدا بيانات منفصلان، عبر `https://192.168.3.86:8443`:
رمز غير مربوط يحتاج موافقة صريحة · تسجيل بتثبيت الشهادة · البصمة مُخفاة في القائمة ·
`--show-id` يطبع نفس البصمة · لاحقة خاطئة تُرفَض · الصحيحة تُوثّق · اتّصال موقّع بجرد
**حقيقيّ: ١١٥ برنامجًا وتحديث معلّق واحد، وانحراف ساعة ٠٫١ ثانية** · وبعد تجديد
الشهادة **رفض الوكيل أن يثق بالجديدة وتوقّف**.

وما لا يُذكر في وعد: الوكيل عمل على **هذا الجهاز**، فما زال إثباته على جهاز فيزيائيّ
ثانٍ غير قائم.

‎+١٠ اختباراتٍ أوّلها **مستمع TLS حقيقيّ في الاختبارات نفسها**. المجموع **٥٦٤**.

## [1.17.0] — 2026-07-26

### أُضيف (Added) — صفحة الوكلاء: الميزة صارت قابلة للاستعمال لأوّل مرّة
بنينا البروتوكول في v1.14–v1.16 بلا واجهة. وبناءُ الواجهة كشف أنّ **الميزة كلّها
كانت غير قابلة للوصول**: لا نقطة تُولّد رمز تسجيل، ولا طريق لفتح المستمع إلّا
بتحرير `config.json` يدويّاً (الإعداد مُدرَج في قائمة السماح وغير موجود في نقطة الإعدادات).

- **صفحة «الوكلاء»** بستّ لغات: حالة المنفذ، وتسجيل جهاز بأمر جاهز للّصق مع
  عدّاد انتهاء، وقائمة الأجهزة بحالتها الصحيّة، وموجز الطلبات المرفوضة، والشهادة.
- **التأكيد صار تحدّياً لا زرّاً.** الرمز غير المربوط يقبله أيّ جهاز على الشبكة،
  فقد يظهر جهاز غريب في القائمة بجوار المقصود. فـ`GET /api/agents` **يُخفي** بصمة
  الوكيل المعلّق بعد أوّل ثمانٍ، والتأكيد يطلب **آخر ثمانٍ** — وهي لا توجد إلّا على
  شاشة الجهاز الهدف. فالإجابة إثباتُ وصولٍ إلى الجهاز الذي يُوثَق، مع حدّ خمس
  محاولات وتسجيل كلّ محاولة فاشلة في سجلّ التدقيق بلا إظهار الجواب الصحيح.
- **`--show-id`**: يطبع بصمة الجهاز بلا شبكة ولا حالة محفوظة. يخدم أمرين: توليد رمز
  **مربوط** بذلك الجهاز (ويصل الوكيل موثّقاً بلا خطوة تأكيد)، واسترجاع جواب
  التحدّي إذا مرّ سطر التسجيل واختفى.
- **موجز الطلبات المرفوضة**: بدونه كلّ فشل يصل المشغّل بوصفه **صمتاً** — المركز لا
  يفتح اتّصالاً نحو الأجهزة. والصفحة تترجم السبب إلى علاج: `clock_skew:412` يصير
  `w32tm /resync`، وموجز فارغ بجوار جهاز صامت يقول صراحةً إنّ الطلب لم يصل أصلاً.
- **«انسَ الجهاز»** (`DELETE`، مشروط بـ`revoked`): الإبطال كان باباً ذا اتجاه واحد —
  نقرة واحدة تمنع جهازاً من التسجيل للأبد بلا مخرج. والتاريخ يبقى في سجلّ التدقيق.
- **تجديد الشهادة يطلب عدد الأجهزة التي سيكسرها** — ليس مربّع تأكيد: اختلاف العدد
  يعني أنّ شاشتك قديمة عن الرقم نفسه الذي يقيس الضرر.

### ما رفضت الصفحة أن تعرضه
**لا أزرار أوامر.** المركز يستقبل **عدد** التحديثات لا قائمة معرّفاتها، والوكيل يرفض
أي معرّف لم يُبلِّغ عنه هو — فزرّ «ثبّت» مضمون الفشل. و«اجرد الآن» يُرجِع `refreshed`
بلا عمل لأنّ الجرد يحدث في كلّ اتّصال أصلاً. **زرٌّ أثرُه الوحيد سطرُ تدقيق يُعلّم
المشغّل أنّ الصفحة تكذب.** والصفحة تقول السبب بدل أن تخفيه. ولافتة دائمة غير
قابلة للإخفاء تذكر أنّ الوكيل يتوقّف عند إعادة التشغيل ولا يُحدّث نفسه. ولا يضيف
التطبيق قاعدة جدار حماية نيابةً عنك — يعرض الأمر لتنفّذه أنت.

‎+٥٤ اختباراً (٣٠ خلفيّة و٢٤ واجهة). المجموع: **٥٥٤ خلفيّة و١٥٥ واجهة**.

## [1.16.0] — 2026-07-26

### أُضيف (Added) — مستمع TLS للوكلاء: **منفصل**، مقصور على ثلاثة مسارات، ومطفأ افتراضيًّا
الطريق البديهيّ — وضع TLS على المستمع الحاليّ وربطه بالشبكة — هو **الخطأ**: ذلك المستمع
يخدم API يعمل بصلاحية مسؤول على جهاز المشغّل (التحديث والتثبيت والإعدادات كلّها).
تعريضه للشبكة لأجل ثلاث نقاط للوكيل مقايضةٌ تُسقِط خاصيّة الأمان الأساسيّة في المنتج.

- **نسخة uvicorn ثانية** بمنفذها الخاصّ (8443) وTLS إلزاميّ، **وقائمة سماح بثلاثة
  مسارات بأسمائها الكاملة** + فحص صحّة. وأي مسار آخر يُردّ بـ**404 قبل المطابقة**، فلا
  تنشر نقطةُ مشغّلٍ تُضاف لاحقًا تحت `/api/agents/` نفسها على الشبكة بالخطأ. ولا توثيق
  تفاعليّ ولا مخطَّط OpenAPI على هذه المنفذ.
- **مطفأ افتراضيًّا:** فتح منفذ إصغاء على جهاز عميل قرارٌ يتّخذه العميل، لا أثر جانبيّ
  للترقية. ويُفعَّل من الإعدادات، وفشل تشغيله **لا يُسقِط التطبيق** (تعارض منفذ مشكلة
  إعداد لا انهيار).
- **شهادة ذاتية التوقيع يملكها المركز**: تُغطّي اسم الجهاز وكل عناوينه (فبصمة مثبَّتة
  لا تنفع إن فشل الاتصال على الاسم)، وIP تُكتَب في حقل IP لا DNS، وليست CA، وصلاحيتها
  ٨٢٥ يومًا. والوكيل يُثبِّت بصمتها — وهذا **أقوى** من «جهةٌ ما وقّعت لهذا الاسم».
- **التجديد صريح**: نقطة منفصلة تُعيد إصدار الشهادة وتُرجِع تحذيرًا نصيًّا بأن **كل
  وكيل مُسجَّل ثبّت الشهادة السابقة وسيحتاج إعادة تسجيل** — تدوير صامت إمّا يكسر الأسطول
  أو يُفرغ التثبيت من معناه.

### أُصلح (Fixed) — حماية كانت تمنع مسار تعافيها
أوّل نسخة من قفل صلاحيات مفتاح الشهادة منحت Administrators وSYSTEM فقط، **فمنعت
التطبيق من استبدال مفتاحه**: التجديد — وهو العمليّة التي يجب أن تعمل عند انتهاء
الصلاحيّة — كان يفشل بـ`PermissionError`. كشفَه اختبار التجديد. صار المنح يشمل الحساب
المُشغِّل، والإصدار الجديد **يحذف قبل الكتابة** بدل الكتابة فوق ملفّ مقيَّد.

‎+١٦‎ اختبارًا أكثرها عن **ما يرفضه هذا المنفذ**: قائمة الفلييت، وإعدادات النظام،
وحزمة الدليل، و`/docs`، و`/openapi.json` — كلّها 404. المجموع **٥٢٤**.

## [1.15.0] — 2026-07-26

### أُضيف (Added) — `HomeUpdater.exe --agent`: نفس الملفّ التنفيذيّ يعمل وكيلًا
الشريحة الثانية من الوكيل: جانب **الجهاز الهدف**. ولماذا نفس الملفّ لا ملفّ ثانٍ؟ لأن
بناءً ثانيًا وتوقيعًا ثانيًا وقناة تحديث ثانية كلٌّ منها موضع يتخلّف فيه وكلاء الأسطول،
**والوكيل المتخلّف أسوأ من لا وكيل**. والوكيل يُعيد استخدام نفس كود الجرد وتحديثات
ويندوز الذي يشغّله المركز محليًّا — فما يراه العميل عن جهازه هو حرفيًّا نفس المسار.

**ما يفرضه الوكيل على نفسه (لا على المركز فقط):**
- **المفتاح الخاصّ يُولَّد على الجهاز الهدف**، ويُعمّى عند الراحة بمفتاح التطبيق
  المحميّ بـDPAPI، **ولا يُرسَل أبدًا** — واختبارٌ يقرأ ملفّ الحالة ويؤكّد أن
  `BEGIN PRIVATE KEY` ليست فيه.
- **مركز بعيد بلا https مرفوض**، وhttps بلا بصمة شهادة مرفوض. وhttp مسموح فقط
  للحلقة المحليّة كي يبقى الاختبار ممكنًا بلا دعوة أسطول إلى نصّ صريح.
- **تغيّر شهادة المركز يُوقِف الوكيل** برسالة صريحة بدل قبول شهادة جديدة عند رؤيتها.
- **لا يُنفّذ معرّفًا لم يُبلِّغ هو عنه**: أمرٌ يسمّي تحديثًا لم يره هذا الجهاز يُرفَض
  ويُبلَّغ بالرفض — فمركزٌ مخترَق لا يستطيع تسمية شيء لهذه الآلة أن تفعله.
- **نوع أمر غير معروف يُرفَض محليًّا أيضًا** (دفاع في العمق: المركز يُعدِّد، والوكيل
  يُعدِّد كذلك، فلا يرتجل وكيلٌ قديم أمام مركز أحدث أو مخترَق).
- **تراجع أُسّيّ عند تعذّر الوصول** بسقف ساعة، فلا حلقة ساخنة على جهاز يُعيد الإقلاع.
- **الانحراف الزمنيّ يُحذَّر منه قبل أن يصير رفضًا**، فيُصلَح قبل أن يصمت الجهاز.

**الاستخدام:** `HomeUpdater.exe --agent --hub <url> --token <رمز التسجيل> [--name]`
للمرّة الأولى، ثم `--agent` وحدها لاحقًا (أو `--agent --once` لفحص واحد — وهذا ما
تستدعيه مهمّة مجدولة، وهو ما يجعل الوضع قابلًا للاختبار بلا خدمة).
والفحص يُجرى **قبل أي شيء رسوميّ** في نقطة الدخول، كي يعمل تحت حساب خدمة في الجلسة 0
حيث لا سطح مكتب أصلًا.

‎+١٤‎ اختبارًا **ضدّ مركز يُسيء التصرّف** لا ضدّ مركز مثاليّ، وكلّها على نقاط المركز
الحقيقيّة عبر عميل الاختبار (لا مركز وهميّ يختبر نفسه). المجموع **٥٠٨**.

**ما زال غير مبنيّ ويُقال صريحًا:** تثبيت الخدمة ودورة حياتها · التحديث الذاتيّ ·
نافذة الموافقة في الجلسة 0 · **ومستمع TLS في المركز** (اليوم يستمع على الحلقة المحليّة
بـHTTP فقط، فوكيل على جهاز آخر يحتاج تلك الشريحة قبل أن يعمل خارج المختبر).

## [1.14.0] — 2026-07-26

### أُضيف (Added) — الوكيل: الجداول والنقاط والمصادقة (الشريحة الأولى)
المركز صار قادرًا على تسجيل وكيل والتحدّث معه بقناة موقَّعة — **بلا منفذ وارد على
جهاز العميل، وبلا كلمة مرور مسؤول مخزَّنة**. وما بُني هنا هو جانب المركز؛ الوكيل نفسه
(الخدمة على الجهاز الهدف) لم يُبنَ بعد ولا يُقال غير ذلك.

**ثلاثة جداول:** `agents` (المفتاح **العامّ** فقط — الخاصّ يُولَّد على الهدف ولا يُرسَل) ·
`agent_commands` (أوامر مُعدَّدة بحمولة مُصنَّفة، **ولا حقل نصيّ في المخطَّط أصلًا**) ·
`agent_nonces` (دفاع إعادة الإرسال، **يُقلَّم عند كل طلب** فلا يصير هدف كتابة مفتوحًا).
والهجرة كُتبت يدويًّا: التوليد التلقائيّ أراد إعادة بناء جداول الجرد الثلاثة، وإعادة
بناء جدول جرد لإضافة جدول غير متّصل به هي الطريقة التي تفقد بها الهجرات بيانات.

**ثمان نقاط** بثلاث للوكيل (`enrol` · `checkin` · `result`) وخمس للمشغّل. ونقاط
الوكيل **ليست مستثناة** من حماية المركز: لها مصادقتها الأقوى — رمز تسجيل ثم توقيع
Ed25519 على الطلب — والمسارات الثلاثة مذكورة **بأسمائها لا ببادئة**، كي لا ترث نقطة
مشغّل مستقبليّة الاستثناء صامتة.

**ما يفرضه `agent_auth`، وكلّه جاء من الهجوم الخصميّ:**
- **حدّ الحجم يُفحَص قبل كل شيء** (٥١٢ كيلوبايت): قراءة «جرد» ضخم ثم اكتشاف أن التوقيع
  خاطئ تعني أن غير المُصادَق يقرّر كم ذاكرة يُنفِق المركز.
- **معرّف الوكيل داخل المُوقَّع** مع الطريقة والمسار والوقت والرقم وهاش الجسم — فالإبطال
  يُسقط معرّفًا لا مفتاحًا، وتوقيعٌ ملتقَط لا يصلح لمسار آخر ولا لهويّة أخرى.
- **الرقم يُخزَّن ويُقلَّم**، والطلب المُعاد يُرفَض بـ`replayed_nonce`.
- **الانحراف الزمنيّ يُقال للوكيل** (`clock_skew:400`) ويُخزَّن في سجلّ الجهاز، فلا يخرج
  جهاز من الأسطول بصمت لأن ساعته منحرفة.
- **وكيل مُبطَل يُرفَض من طلبه التالي**، ولا يستطيع **إعادة تسجيل نفسه** ليعود.

**ونتيجة مباشرة لعيب الأمس:** رمز غير مربوط بجهاز يُنتج وكيلًا **`pending`** لا يتلقّى
أي أمر حتى يؤكّده مشغّل بشريّ بمقارنة البصمة على الجهاز — وإلّا كانت خطوة التأكيد زينة.
و`agent_checkin` يُسجَّل **مُلخَّصًا** (عدد المنتجات لا قائمتها) لأن `verify()` يقرأ
السلسلة كلّها، وأسطولٌ يكتب جرده كل دقيقة يجعل التحقّق مستحيلًا عمليًّا.

‎+٢٨‎ اختبارًا، **كلّها هجمات يجب أن تفشل**: توقيع بمفتاح آخر · توقيع مُعاد على مسار
آخر · إعادة إرسال · طابع زمنيّ قديم · معرّف مجهول · نفس المفتاح بهويّتين · جسم ضخم ·
أمر لجهاز آخر يُغلقه وكيل ثالث · نوع أمر غير معروف (`run_shell` مثلًا) · وكيل مُبطَل.
المجموع **٤٩٤**.

## [1.13.1] — 2026-07-26

### أُصلح (Security) — رمز التسجيل لم يكن مربوطًا بالجهاز، والوثيقة تقول إنه مربوط
قبل كتابة سطر واحد من الوكيل، هاجم **٥٣ وكيلًا** تصميمَ البروتوكول: ٤٨ نتيجة، صمدت
**١١** بعد مراجعة خصميّة. وأخطرها لم يكن في التصميم بل في **كود مشحون منذ v1.10**:

- **`enrolment.redeem()` لم يكن يقارن الجهاز بأي شيء.** `mint()` يوقّع
  `{nonce, expires_at, target_hint}`، و`target_hint` يُكتب ولا يُقرأ أبدًا؛ فالدالة
  تهشّم ما يُعطى لها وتُعيده بوصفه «بصمة الجهاز المُسجَّل». ومع ذلك كان **docstring**
  الوحدة و**AGENT_PROTOCOL.md** و**AGENT_SPIKE.md** (تحت «ما أُثبِت فعلًا ✅») تقول إن
  «رمزًا مُعترَضًا لا يُستهلَك من جهاز آخر». الاختبار نفسه كان يُظهر ذلك ولم يلحظه أحد:
  استهلاك بجهاز ثم بجهاز آخر يفشل بـ`already_redeemed` لا `wrong_machine`.
  **الأثر:** رمزٌ يُسرَّب (لقطة شاشة، تذكرة، محادثة) كان صالحًا من أي جهاز؛ ولأن نقطة
  `/api/agents/enrol` لم تُبنَ بعد فالضرر اليوم: ادّعاء كاذب في وثيقة قرار، **وتعطيل**
  تسجيل الجهاز الحقيقيّ (السارق يستهلك الرقم) — ويصير استغلالًا كاملًا لحظة بناء النقطة.
- **الإصلاح:** البصمة صارت **داخل الحمولة الموقَّعة عند الإصدار**، و`redeem` يرفض
  `wrong_machine` **دون أن يستهلك الرقم** فيبقى الجهاز الحقيقيّ قادرًا على التسجيل.
  ورمزٌ غير مربوط ما زال ممكنًا لهدف مجهول لكنه يحتاج `allow_any_machine=True` صريحة،
  وتُعلَّم نتيجته `bound: false` كي لا يُعامَل الوكيل الناتج معاملة موثوق.
- **أحاديّة الاستخدام تنجو من إعادة التشغيل:** الأرقام المستهلَكة انتقلت من مجموعة في
  الذاكرة إلى ملفّ يُكتَب ذرّيًّا ويُقلَّم عند انتهاء الصلاحيّة، و**الاستهلاك الذي يتعذّر
  تسجيله يُرفَض** (فشل مغلق) — لأن منح تسجيل لا نستطيع تسجيله يجعل «أحاديّ الاستخدام» وعدًا.
- **الوثائق صُحِّحت في ثلاثة مواضع** مع بقاء **تصحيح مؤرَّخ مكتوب** فيها: وثيقة قرار
  أخطأت مرّة يجب أن تقول أين أخطأت.
- ‎+٥‎ اختبارات، منها الناقص: **رمز صحيح + جهاز خاطئ ← رفض**، وإعادة تشغيل حقيقيّة
  (`importlib.reload`) لا مجرّد مسح حالة. المجموع **٤٦٦**.

### أُضيف (Docs) — تصميم بروتوكول الوكيل، مُهاجَمًا قبل تنفيذه
`03_الوثائق/AGENT_PROTOCOL.md`: نموذج تهديد صريح، وهويّة بمفتاح يُولَّد على الجهاز
الهدف ولا يُرسَل، وتوقيع يغطّي **معرّف الوكيل والطريقة والمسار والوقت والرقم وهاش الجسم**،
وأوامر **مُعدَّدة بلا أي حقل نصيّ**، ولا منفذ وارد على جهاز العميل. وما صمد من الهجوم
صار **شروطًا** مكتوبة: حدّ حجم يُفرَض قبل التحقّق · كاتب واحد لسلسلة التدقيق · سياسة
نموّ للسجلّ · مسار استرداد لتثبيت شهادة TLS · الانحراف الزمنيّ يُبلَّغ لا يُسقِط بصمت.
وقائمة صريحة بما **ليس** في النسخة ١ (تحديث ذاتيّ، موافقة في جلسة المستخدم، تثبيت
الخدمة، winget كـSYSTEM).

## [1.13.0] — 2026-07-26

### أُضيف (Added) — الجدول يغطّي برامج العملاء، وينمو بلا إصدار
الجدول كان يغطّي ما يُصادف وجوده على جهاز مطوّر واحد؛ فعميل في عيادة أو مكتب محاسبة
كان سيرى تغطية شبه صفريّة للبرامج التي تهمّه.

- **٢١ خريطة جديدة** (المجموع **٤١**، كان ٢٠): Firefox · Thunderbird · LibreOffice ·
  VLC · WinRAR · Notepad++ · PuTTY · FileZilla · PowerToys · Telegram · Postman ·
  Audacity · GIMP · Malwarebytes · TeamViewer · Zoom · PostgreSQL · MySQL ·
  Docker Desktop · Temurin · Google Drive.
  كل خريطة قرارها مبنيّ على **سحب حقيقيّ لقاموس CPE من NVD**، ثم مرّت على **مراجع
  خصميّ مهمّته دحضها** (٣٦ وكيلًا: ٣٣ قرارًا، ١١ رُفِض). وأمثلة على ما منعه ذلك:
  عميل Zoom المكتبيّ اسمه `zoom:workplace_desktop` لا `zoom:zoom`؛ ومصنّع Notepad++
  هو `don_ho`؛ وLibreOffice تحت `libreoffice` لا `documentfoundation`؛ وFileZilla
  تحت `filezilla-project` مع تمييز العميل عن الخادم.
- **٩ استثناءات موثَّقة جديدة** بأسبابها المقيسة: Slack (كلمة 'slack' تُرجِع ٣٠٤ سجلًّا
  ولا واحد منها تطبيق Slack) · OBS Studio (صفر) · Discord (سجلّان أحدهما بلا إصدار) ·
  Steam (الإصدار خليط تاريخ وطابع زمنيّ) · SQL Server (سنوات إصدار) · Visual Studio ·
  Oracle JRE (٥٢٥ سجلًّا بثلاث نسخ مميّزة فقط) · Skype · Dropbox.
- **تصحيح في منهجي:** سحبي الأوّل أعاد صفرًا لـNotepad++ وSlack وOBS وZoom لأنني خمّنتُ
  معرّف CPE. **صفرٌ من معرّف خاطئ ليس دليل غياب** — فأعدتُ السحب بكلمة مفتاحيّة، فتغيّرت
  أربعة قرارات: Notepad++ وZoom وGoogle Drive صارت خرائط، وSlack وOBS ثبت غيابهما فعلًا.

### أُضيف (Added) — طبقة خرائط الموقع
`%APPDATA%\HomeUpdater\data\cpe_overrides.json` تُقرأ عند كل فحص، فيضيف العميل خريطة
لبرنامجه الخاصّ بلا انتظار إصدار — وبلا أن يتسرّب تخمين إلى تقرير مدفوع:
- كل مدخلة تحمل **`verified_by` و`verified_on` و`evidence`** تصف ما أعادته NVD،
  وإلّا رُفِضت عند التحميل.
- الثقة **مسقوفة عند medium** والمصدر `site` — فالتقرير يُميّز خريطة العميل عن خرائطنا.
- **لا يُمكن نقض استثناء موثَّق**، و**خرائط التطبيق تفوز دائمًا** على ملفّ الموقع.
- ملفّ تالف = صفر خرائط وسطر في السجلّ، لا سقوط ولا تعطيل صامت للتغطية.
- وثيقة كاملة: `03_الوثائق/CPE_OVERRIDES.md` (كيف تتحقّق قبل أن تكتب، وأمثلة واقعيّة
  على أخطاء المصنّعين، والتنبيه أن صفرًا من معرّف خاطئ ليس دليل غياب).
- ‎+٢١‎ اختبارًا لهذه الطبقة وحدها (المجموع **٤٦٢**).

### مُتحقَّق حيًّا
استعلمتُ NVD بأسماء CPE التي تُنتجها الخرائط الجديدة: Firefox 142.0.1 أعاد **٨١**
سجلًّا و**٥ ثغرات محتسَبة**، وPuTTY 0.83 أعاد **٣ محتسَبة**. والبقيّة `total=0` أي
**«لا بيانات لهذا الإصدار» لا «سليم»** — وهو ما يقوله التقرير حرفيًّا.

## [1.12.0] — 2026-07-25

### أُضيف (Added) — بوّابة تثبيت في CI · مفتاح NVD · مستند مطبوع للمدقّق

**١) الإصدار يُثبِّت نفسه قبل أن ينشر.** عيبان وصلا الإنتاج في يوم واحد (ترقية
v1.10.4 أُلغِيت على ملفّ مقفل فحذف تراجُعها واجهة التطبيق، وv1.11.0 شحنت استثناءً
بالاسم الكامل مع الإصدار) — وكلاهما اكتُشف **يدويًّا على جهاز حقيقيّ** لأن الاختبارات
تفحص بايثون لا الحمولة المُثبَّتة. صارت وظيفة الإصدار: تُثبِّت المثبّت صامتًا، وتتحقّق
أن رقم السجل = رقم البناء = ملفّ VERSION، وأن **كل حزمة يشير إليها `index.html`
موجودة فعلًا** (وهو بالضبط عطب v1.10.4)، وأن كل قطعة تُحمَّل عند الطلب موجودة، وأن
حمولة PyInstaller تحتوي وحداتنا، وأن **لا حزمة زائدة** بقيت (التنظيف يعمل)، ثم
تُزيل التثبيت وتتأكّد أن السجل والملفّات ذهبت. أي فشل يمنع النشر.

**٢) مفتاح NVD (اختياريّ، مجّانيّ).** الحدّ المجهول ٥ طلبات/٣٠ث ضربنا **٤ مرّات** في
يوم واحد فظهرت منتجات بحالة «لم تُفحَص بعد». صار في **الإعدادات** حقل للمفتاح يرفع
الحدّ إلى ٥٠، فينزل الفاصل بين الطلبات من ٦٫٥ ثانية إلى ٠٫٧. والمعاملة معاملة سرّ:
لا يُعاد إظهاره أبدًا (الخادم يُرجِع «محفوظ؟» فقط)، ولا يُسجَّل، ويُستبدَل في سجلّ
التدقيق بـ`[set]`، والخانق يقرأ الحدّ الجديد **أثناء التشغيل** فأوّل فحص بعد اللصق
يستفيد منه.

**٣) مستند مطبوع لحزمة الدليل** (`GET /api/evidence/pack.html`) وزرّ «طباعة / PDF».
قرار صريح: **لا نُولّد PDF**. لا محرّك PDF في البناء ولا خطّ عربيّ مضمَّن، وPDF بعربيّة
غير مُشكَّلة أسوأ من لا شيء؛ فالمستند HTML مستقلّ (RTL، بأنماط طباعة) يفتحه المتصفّح
— ومنه WebView2 داخل التطبيق — فيُشكِّل العربية صحيحًا ويحفظه PDF بضغطة. يحتوي:
الغلاف والنطاق والمنهج، وجدول التغطية بتفصيل أسبابها، وجدول الثغرات مع **سند كل
واحدة**، وجدول ما فُحِص ولم يُحتسَب بسببه، وجدول غير المطابَق **باسم المنتج لا معرّفه**،
والتحديثات المُطبَّقة، وحالة سلسلة التدقيق وبصمتها، وبصمة المحتوى، وحدود التقرير.
وإن كانت السلسلة مكسورة يقول المستند ذلك بالأحمر مع رقم السجلّ.

### أُصلح (Fixed)
- **قائمة «غير مطابَق» في الحزمة كانت بلا اسم منتج** — معرّفًا فقط، فيقرأ المدقّق
  `ARP\Machine\X64\O365HomePremRetail - ar-sa` بدل «Microsoft 365». صار الاسم مُدرَجًا.
- **هروب HTML** لكل ما يدخل المستند: أسماء المنتجات تأتي من سجلّ ويندوز، والملفّ
  يفتحه العميل في متصفّح — واختبار يزرع `<script>` في اسم منتج ويؤكّد أنه لا يعمل.
- ‎+٧‎ اختبارات backend (المجموع **٤٤١**).

## [1.11.1] — 2026-07-25

### أُصلح (Fixed) — استثناءات تنهار عند كل تحديث (ظهر في تقرير حقيقيّ)
صدّرتُ حزمة الدليل من بيانات هذا الجهاز بعد ترقيته إلى 1.11.0، فقال التقرير
**«١ لم يُدرَس»** — والمنتج هو **HomeUpdater نفسه**. السبب: كتبتُ استثناءه في 1.11.0
بالاسم الكامل «HomeUpdater 1.10.6»، والاسم يحمل رقم الإصدار، فبطل الاستثناء لحظة
الترقية. وثمانية استثناءات أخرى كانت مكتوبة بالطريقة نفسها (Afterburner، NVIDIA App،
FrameView، صوت HD، Windows SDK، سائق Wifi6…) أي أنها كانت ستنهار واحدًا واحدًا.

- الاستثناءات صارت **قواعد مُرسّاة لا أسماء كاملة**: نمط على بداية الاسم بلا رقم
  إصدار، أو لاحقة معرّف حين يكون الاسم مُعرَّبًا (سائق صوت NVIDIA).
- **تطبيقنا يُستثنى بمعرّف AppId الثابت** (`{8F3A1C7E-…}_is1`) الذي يُثبّته سكربت
  المثبّت صراحةً ولا يتغيّر بين الإصدارات — لا باسمه الذي يحمل الإصدار.
- كل قاعدة تحمل **مثالًا حقيقيًّا مُلاحَظًا** (`sample`)، واختبار جديد **يُرقّي إصدار
  المثال ويؤكّد أن التصنيف لم يتغيّر** — فلا يتكرّر هذا الخلل بصمت.
- **ترتيب التصنيف صُحِّح:** حِزم المتجر تُصنَّف **قبل** قواعد الأسماء، لأن كون الإصدار
  هوية حِزمة حقيقةٌ بنيويّة؛ وبدون ذلك كانت قاعدة «Microsoft 365» المكتوبة لأوفيس
  المكتبيّ تلتقط تطبيق المتجر «Microsoft 365 Copilot» وتعطيه سببًا لا يخصّه (مقيس).
- ‎+٢‎ اختبارات backend (المجموع **٤٣٦**). النتيجة على هذا الجهاز: ٢٥ مُطابَق · ٥٧ حزمة
  متجر · ٣٣ استثناء موثَّق · **٠ لم يُدرَس**، وتغطية القابل للمعالجة **100%**.

## [1.11.0] — 2026-07-25

### أُضيف (Added) — رفع التغطية، وتصنيف أمين لما لا يمكن تغطيته
قياس على جرد هذا الجهاز (١١٥ منتجًا): كانت التغطية **٢٠ منتجًا (17.4%)** و٩٥ منتجًا
بسبب واحد باهت هو «لا تعريف CPE». التفكيك الحقيقيّ كشف أن الرقم كان يُساء فهمه:
**٥٧** من الـ٩٥ حِزم متجر (MSIX) رقمها هوية حِزمة لا يتعقّبها NVD أصلًا.

- **٥ خرائط جديدة، كل واحدة بدليل من قاموس CPE في NVD** (سحبتُ القاموس فعليًّا):
  Node.js (`nodejs:node.js` — ١٦٥٨ مدخلًا) · USBPcap (`usbpcap_project:usbpcap` —
  والإصدار المثبَّت 1.5.4.0 موجود بالحرف) · Npcap (`nmap:npcap`) · UniGetUI
  (`devolutions:unigetui` — العنوان في NVD «Devolutions UniGetUI») · برنامج تشغيل
  رسومات NVIDIA (`nvidia:gpu_display_driver`). النتيجة: **٢٥ منتجًا مُطابَقًا (21.7%)**.
- **طبقة مطابقة جديدة للمنتجات المُكتشَفة من السجل**، لأن معرّف ARP لمثبّتات MSI
  يحمل ProductCode **يتغيّر مع كل إصدار** (مقيس على Node.js): فالمطابقة بالاسم
  (نمط مُرسَّى) أو بلاحقة المعرّف حين يكون الاسم مُعرَّبًا (سائق NVIDIA اسمه هنا
  «برنامج تشغيل الرسومات»). واختبار يمنع إدخال أي معرّف ARP في الجدول المُنسَّق،
  لأن خريطة كهذه تتعطّل بصمت عند أوّل تحديث — أسوأ أنواع الفشل.
- **١٤ استثناءً موثَّقًا جديدًا بأسبابها المقيسة:** MSI Afterburner (الإصدار
  «4.6.7 Beta 2» غير قابل للمقارنة) · uTorrent Web (مدخل بلا إصدار فيُطابق كل شيء) ·
  Microsoft 365 (NVD يرقّم 2401.x والتثبيت يقول 16.0.x) · Windows SDK (NVD 10.0.26100.x
  والسجل 10.1.26100.x، وتحويل 10.1←10.0 تخمين) · NVIDIA App/FrameView/صوت HD ·
  سائق محوّل Wifi6 · فحص حالة الكمبيوتر · مثبّت Visual Studio · إضافة اجتماعات Teams ·
  و**HomeUpdater نفسه** (لا CPE له، والإبلاغ عن نفسه دوريّ).
- **التغطية صارت تُشرَح لا تُختصَر في رقم:** النسبة الخام تبقى العنوان، ويُضاف تحتها
  تفصيل الأسباب — حِزم متجر / استثناءات موثَّقة / **لم تُدرَس بعد** — مع نسبة التغطية
  من **القابل للمعالجة** بجانبها لا بدلًا عنها. على هذا الجهاز: ٢٥ مُطابَق · ٥٧ حزمة
  متجر · ٣٢ استثناء موثَّق · **٠ لم تُدرَس** → أي أن كل منتج في الجرد صار إمّا
  مُطابَقًا دقيقًا أو له سبب مكتوب.
- سبب عدم المطابقة صار مُحدَّدًا في الواجهة والحزمة (`store_package` /
  `documented_exclusion` / `version_not_comparable` / `no_cpe_mapping`) بست لغات،
  بدل سبب واحد يخلط «بحثنا ولا يوجد» مع «لم يبحث أحد».
- ‎+٩‎ اختبارات backend (المجموع **٤٣٤**).

### تصحيح
قلتُ إن رفع التغطية إلى **٤٠–٥٠%** ممكن. **خطأ**: على هذا الجهاز السقف الحقيقيّ نحو
**٢٢–٢٤%** لأن ٥٧ من ١١٥ حِزم متجر لا يتعقّبها NVD، ولا يجوز تعريفها. الرقم الذي
يُحسَن قياسه هو تغطية **القابل للمعالجة** — وهو الآن ١٠٠%.

## [1.10.6] — 2026-07-25

### أُصلح (Fixed) — مسار الترقية: من «فشل مُدمِّر» إلى «فشل بلا أثر»
تدقيق بـ٣٧ وكيلًا (٤ عدسات + مراجعة خصميّة لكل نتيجة) على مسار الترقية: ٣٣ نتيجة،
صمدت ٢١ وسقطت ١٢. أخطرُها كان في تغييرَين أدخلتُهما في هذه الجلسة نفسها.

- **`InstallDelete` أُزيل، والتنظيف انتقل إلى ما بعد نجاح النسخ** (`ssPostInstall`).
  كان يحذف `frontend_dist\assets` **قبل** النسخ، وتراجُع Inno لا يُعيد ما حذفه —
  فأي فشل بعد تلك اللحظة يترك تطبيقًا مُثبَّتًا **بلا واجهة**، وهو ما حدث فعلًا في ترقية
  v1.10.4. الآن التثبيت الفاشل **لا يترك أثرًا** والحزم القديمة تبقى في مكانها.
  والتنظيف يُبقي كل ملفّ يُشار إليه من `index.html` **أو من داخل حزمة مُشار إليها**،
  لأن حزمة الدخول تُحمِّل قطعًا عند الطلب (منها قطعة الـPDF ‎٦ ميغابايت‎) — ويفشل مُغلَقًا:
  إن تعذّرت قراءة `index.html` لا يُحذف شيء.
- **حرس قبل أي تغيير:** `PrepareToInstall` تتحقّق أن `adb.exe` صار قابلًا للاستبدال (فتح
  حصريّ)، وإن بقي مقفلًا **توقف التثبيت قبل تعديل أي ملف** برسالة صريحة بالعربية
  والإنجليزية ورمز خروج غير صفريّ.
- **`msedgewebview2` اليتيم** يُقتَل مع adb (نفس عائلة القفل، عمليّة أخرى)، والمطابقة
  صارت بـ`StartsWith` لا `-like` فلا يكسرها حرف بدل في مسار تثبيت مخصَّص.
- **المُزيل كان فيه نفس العيب معكوسًا:** أُضيف إيقاف adb وWebView2 من مجلَّد التثبيت إلى
  `UninstallRun`، فلا تُترك ملفّات بعد إزالة صامتة.

### أُصلح (Docs) — وثيقة النشر المؤسّسيّ
- **`/SUPPRESSMSGBOXES` أُعيد إلى كل أوامر النشر غير التفاعليّ** مع شرح السبب: Intune
  وسكربت إقلاع GPO يعملان بحساب System في **الجلسة 0**، حيث صندوق Abort/Retry/Ignore
  **لا يراه أحد**؛ فإزالتي للمفتاح في v1.10.5 كانت ستحوّل الفشل السريع إلى **تعليق** حتى
  مهلة أداة النشر، والقتل القسريّ بعدها يتخطّى التراجُع فلا رمز خروج ولا سجلّ. القاعدة
  الصحيحة: استخدمه في النشر غير التفاعليّ **وراقب رمز الخروج** (`5` = أعِد المحاولة).
- **قاعدة كشف Intune كانت تُبطل كل ترقية لاحقة:** `DisplayVersion ≥ 1.5.10` تُقرأ
  «مُثبَّت» لكل إصدار قادم، فيتخطّى Intune الترقية. صار مذكورًا صريحًا أن الرقم يجب أن
  يساوي الإصدار المنشور ويُحدَّث معه؛ وأُزيل البديل «كشف بوجود `HomeUpdater.exe`» لأنه
  يُبلِّغ بالنجاح على تثبيت معطوب.
- أسماء الملفّات في كل الأوامر صارت بالإصدار الحقيقيّ (كانت `1.5.10` وهو لم يُبنَ قطّ)،
  وأُفصِح أن الإزالة **تُبقي** القاعدة والإعدادات والسجلّات و**مفتاح التشفير** في
  `%APPDATA%\HomeUpdater`، وأن التطبيق يطلب صلاحية مسؤول **في كل تشغيل**، وأن WebView2
  شرط لا يفحصه المثبّت، وأن ARM64 يعمل بمحاكاة غير مُختبَرة.

## [1.10.5] — 2026-07-25

### أُصلح (Fixed) — ترقية فاشلة تترك التطبيق **بلا واجهة** (حادثة مقيسة، لا احتمال نظريّ)
ترقية هذا الجهاز إلى v1.10.4 فشلت في منتصفها. النصّ من سجلّ Inno:
`_internal\platform-tools\adb.exe — DeleteFile: file in use (5). Retrying` ×٤ ثم
`Defaulting to Abort for suppressed message box` ثم `Rolling back changes` ثم
`Deleting directory: ...\frontend_dist\assets`. النتيجة: تطبيق مُثبَّت **بلا أي واجهة**
ورمز خروج ‎5‎.

- **السبب:** adb يُشغِّل خادمًا **يفصل نفسه**، فيبقى حيًّا بعد إغلاق التطبيق ويقفل
  `adb.exe`؛ و`taskkill /T` على التطبيق لا يمسّه لأنه ليس ابنًا له.
- **الإصلاح:** `StopAdbServer` جديد يُنفَّذ في `PrepareToInstall` **بلا شرط** (قبل أي نسخ)،
  لأن الخادم قد يكون قائمًا والتطبيق مُغلق أصلًا. ويقتل **فقط** عمليّات adb التي
  صورتها داخل مجلَّد التثبيت — لا `taskkill /IM adb.exe` ولا `adb kill-server`، فلا
  يُمَسّ adb الذي يستخدمه المطوّر لعمله الخاصّ.
- **الوثيقة:** أُزيل `/SUPPRESSMSGBOXES` من كل أوامر النشر الموصى بها في
  `installer/DEPLOYMENT.md`، مع تحذير صريح: كتم الصناديق يحوّل «ملفّ مقفل — أعد
  المحاولة» إلى **إلغاء وتراجُع مُدمِّر**. فمن يستخدمه لا بدّ أن يراقب رمز الخروج ويعيد
  التثبيت عند ‎5‎، لأن الفشل هنا لا يترك التطبيق كما كان بل يتركه معطوبًا.

### تصحيح
قلتُ أوّلًا إن الوضع الصامت لا يُغلق التطبيق لأن الإغلاق في صفحة المعالج — وهذا **خطأ**:
`PrepareToInstall` موجودة في السكربت من قبل وتعمل في الوضع الصامت أيضًا؛ كشف الخطأَ
مُصرِّف Inno نفسه (تعريف مكرَّر). الفجوة كانت **خادم adb وحده**، لا مسار الإغلاق.

## [1.10.4] — 2026-07-25

### أُصلح (Fixed) — إيجابيّتان كاذبتان في صميم «المطابقة الدقيقة»، ظهرتا على جهاز حقيقيّ
شغّلتُ الميزة على جرد هذا الجهاز (١١٥ منتجًا) فأعطت **٩ ثغرات**؛ فحصتُ كل واحدة
بسندها فسقطت أربع منها. الأثر بعد الإصلاح على نفس البيانات: **٥ ثغرات محتسَبة**
و**٩ سجلّات معروضة غير محتسَبة، كلٌّ بسببه**.

- **منصّة غير مصابة كانت تُحتسَب ثغرة.** NVD يُدرج في بعض السجلّات منتجًا بوصفه
  المنصّة التي يعمل عليها المنتج المصاب، بعلامة `vulnerable: false`. فكان
  CVE-2020-29396 — وهو خلل في **Odoo** — يُنسَب إلى Python 3.14.6 بدرجة **HIGH 8.8**.
  الآن: أي عقدة `vulnerable: false` لا تُحتسَب، ولو ذُكر منتجنا فيها مرّتين
  فتُقدَّم العقدة المصابة صراحةً.
- **مقارنة رقم إصدار بتاريخ.** سجلّات FFmpeg تحدّ الإصابة بتاريخ لقطة
  (`< 2025-01-13`)، والمقارنة الرقميّة تجعل ‎8.1.2‎ «أقدم» من سنة ٢٠٢٥،
  فانطبق CVE-2025-25468/25469 على بناء من ٢٠٢٦. الآن: اختلاف نظام الترقيم
  (تاريخ مقابل إصدار) يُصنَّف `scheme_mismatch` ولا يُحتسَب.
- **مُصنِّف واحد للشاشة وللحزمة المدفوعة** (`cve.counted_and_uncounted`): يستحيل أن
  يختلف رقمٌ يراه العميل على الشاشة عن رقمٍ في الملفّ الذي يشتريه. ويُعاد التصنيف
  عند كل قراءة، فذاكرة NVD المكتوبة قبل هذه القاعدة تُصحَّح بلا انتظار ٢٤ ساعة.
- **لا حذف صامت:** كل سجلّ مستثنى يظهر مع سببه بثلاث حالات مترجَمة في اللغات الست:
  «بلا حدود إصدار» و«منصّة غير مصابة» و«حدود بتاريخ لا بإصدار».
- ‎+٨‎ اختبارات backend (المجموع **٤٢٥**) تُثبِّت الحالتين الحقيقيّتين بأسمائهما،
  واختبار الواجهة يتحقّق من ظهور الأسباب الثلاثة (**١٣١** اختبار واجهة).

## [1.10.3] — 2026-07-25

### أُصلح (Fixed) — ملاحظتان ظهرتا أثناء ترقية حقيقيّة إلى v1.10.2
- **المثبّت كان يُخلّف كل حزم الواجهة القديمة إلى الأبد.** Vite يُسمّي كل حزمة ببصمة
  محتواها، فكل ترقية تكتب أسماء جديدة ولا تحذف السابقة. القياس الفعليّ على هذا الجهاز
  عند v1.10.2: مجلَّد `_internal\frontend_dist\assets` فيه **١٥ ملفًّا بحجم ١٠
  ميغابايت، الحيّ منها ٣ فقط** — أي **١٢ ملفًّا ميتًا بحجم ٣٫٦ ميغابايت** (تتبّعتُ ما
  تشير إليه `index.html` والحزم نفسها، لا التواريخ). أُضيف `[InstallDelete]` يُنظّف
  المجلَّد قبل النسخ، فلا تراكم ولا احتمال تحميل حزمة قديمة. لم يكن خللًا ظاهرًا
  للمستخدم — لكنه ينمو بلا حدّ مع كل إصدار.
- **توثيق حقيقة مهمّة للنشر المؤسسيّ:** الشهادة الذاتيّة **تُولَّد من جديد في كل بناء**،
  فبصمتها تتغيّر مع كل إصدار (v1.10.1: `E395B408…` مقابل v1.10.2: `E329712D…`).
  عمليًّا: إدراج الشهادة في قائمة سماح WDAC/AppLocker **سيتعطّل عند الإصدار التالي**،
  ولا تُبنى سمعة SmartScreen لأن كل بناء «ناشر جديد». الحلّ سرّ `SIGNING_PFX_BASE64`
  بشهادة ثابتة (والأفضل OV/EV). أُضيف تحذير صريح في `installer/DEPLOYMENT.md`.

## [1.10.2] — 2026-07-25

### أُصلح (Fixed) — عيبان ظهرا في تجربة حقيقيّة للميزة على هذا الجهاز
- **تجميع الشركاء كان يُضاعف الأرقام عند استيراد الحزمة نفسها مرّتين.** سحب مجلَّد
  فيه نسختان من ملفّ الموقع كان يُحتسَب موقعين، فتتضخّم الإجماليات بلا أن يلاحظ أحد.
  الآن: تُطابَق البصمة، والنسخة المكرَّرة **تُحتسَب مرّة واحدة وتُعرَض في المرفوضات بسببها
  الصريح** («نفس الحزمة مستوردة أكثر من مرّة») — لا حذف صامت ولا تضخيم صامت.
- **حقل الإجمالي كان يُسمّى `devices` وهو يَعدّ المنتجات لا الأجهزة.** الحزمة تغطّي
  جهازًا واحدًا وتَعدّ برامجه، فجهازان بـ١١٥ منتجًا كانا يظهران في JSON/CSV كـ«٢٣٠
  جهازًا». صار الاسم `products` في الحقل وفي ترويسة CSV — والواجهة كانت تقول
  «إجمالي المنتجات» أصلًا، فالخلل كان في الملفّ المُصدَّر الذي يفتحه المشتري.
- تحسين صياغة العربية في عدّادَي التجميع: «جمّع الحزم المختارة (٣)» و«حزم مرفوضة: ٢»
  بدلًا من «٣ حزمة» و«٢ حزمة مرفوضة» — العدد لا يفسد الجملة الآن.
- ‎+٣ اختبارات backend (التكرار، وموقعان متطابقا الأرقام يُحتسبان كليهما، وترويسة CSV)
  ومطالبتان جديدتان في اختبار الواجهة. المجموع: **٤١٧** backend + **١٣١** واجهة.

## [1.10.1] — 2026-07-25

### أُضيف (Added) — واجهة حزمة الدليل والترخيص
- صفحة **«حزمة الدليل»** جديدة في الشريط: تفعيل الترخيص، ومعاينة مجانيّة، وتصدير
  الحزمة (JSON/CSV)، وتجميع مواقع الشركاء — فصار البيع ممكنًا بلا أوامر API.
- **الترخيص:** لصق المفتاح وتفعيله، وعرض الطبقة والمرخَّص له وتاريخ الانتهاء، وسبب
  الرفض صريحًا (توقيع غير صالح / منتهي / مفتاح غير صحيح)، وإزالة المفتاح.
- **المعاينة مجانيّة قبل الدفع:** عدد المنتجات، و**نسبة التغطية مع كم منها قابل
  للمطابقة الدقيقة**، وعدد الثغرات المنطبقة، وحالة سلسلة التدقيق — فالقيمة والنقص
  كلاهما مرئيّ قبل الشراء. وزرّا التصدير معطَّلان بوضوح حتى التفعيل.
- **الصدق مبنيّ في الشاشة لا في الوثائق فقط:**
  - تُذكَر البصمة بأنها هاش محتوى و**ليست توقيعًا رقميًّا** ولا تُثبِت هوية المُصدِر.
  - يُقال صريحًا إن «لا نتائج» **لا تعني خلوّ المنتج من الثغرات**، وإن التقرير **ليس
    شهادة اعتماد**، وإن التغطية جزئيّة.
  - إن كانت **سلسلة التدقيق مكسورة** يظهر تحذير أحمر يذكر رقم السجلّ ويقول إن
    التقرير المبنيّ عليها لا يصحّ الاعتماد عليه قبل معرفة السبب.
- **تجميع الشركاء في الواجهة:** استيراد ملفّات الحزم، وجدول لكل موقع (منتجات/تغطية/
  ثغرات/حالة السلسلة)، و**الحزم المرفوضة تُعرَض بسببها** مع تنبيه أنها مُستثناة من كل
  الأرقام. متاح لطبقة الشريك فقط، والملفّ غير الصالح **يُسمّى** ولا يُتخطّى بصمت.
- التنزيل يتمّ عبر Blob (الرابط المجرَّد لا يحمل ترويسات الجلسة فيفشل) — أُضيف
  `apiFetchText` و`downloadText` لهذا الغرض.
- ترجمة كاملة في اللغات الست، و‎+١٠‎ اختبارات واجهة (المجموع **١٣١** في ١٨ ملفًّا).

## [1.10.0] — 2026-07-25

### أُضيف (Added) — تجميع الشركاء (بلا لوحة سحابية، تعمّدًا)
- **الشريك يُجمِّع بالاستيراد لا بالاتصال:** كل موقع يُصدِر حزمة دليله، والشريك يستوردها
  ليحصل على عرضٍ واحد لكل مواقعه. هذا يحفظ وعد المنتج (لا تخرج البيانات من شبكة
  العميل)، ولا يحتاج خادمًا متعدّد المستأجرين، وهو ما يطلبه فعلًا مزوّد يخدم ١٥ مكتبًا:
  **مكان واحد يُظهر مَن غير ملتزم**.
- **الجزء الأمنيّ هو التحقّق:** كل حزمة مستوردة **تُعاد تجزئتها** وتُقارَن ببصمتها،
  فالحزمة المُعدَّلة بعد إصدارها **تُرفَض ولا تُدمَج في الأرقام**. تجميعٌ يُمرِّر مُدخلًا
  مزوَّرًا أسوأ من لا تجميع، لأنه يمنح التزوير مصداقية «المجموع». والمرفوضة تُطبَع في
  نفس ملفّ CSV — فالشريك **يرى** الاستثناء ولا يستلم قائمة أقصر بصمت.
- **الترتيب يخدم القرار:** المواقع ذات **سلسلة تدقيق مكسورة تتقدّم** على ذات النتائج
  الكثيرة — لأن تقريرًا باطلًا أخطر من خبرٍ سيّئ. و`chain_ok=NO` تُطبَع صريحة.
- `POST /api/evidence/rollup` و`rollup.csv` — **لطبقة الشريك فقط** (طبقة موقع واحد
  لا تفتح منتج المواقع المتعدّدة، مُثبَّت باختبار).
- **حدّ مُعلَن:** البصمة تُثبِت تطابق النسخة مع الصادرة **لا هوية المُصدِر**؛ فمن يُعدّل
  الحزمة **ويُعيد بصمها** يمرّ. لمنع ذلك يلزم توقيع رقميّ بمفتاح — والمنتج لا يزعمه.
  (اختبار يُثبِّت هذا الحدّ صريحًا بدل تجاهله.)

### سبايك الوكيل — تقييم جدوى، لا وكيل
- **أُنجز وأُثبِت:** آلية **تسجيل الوكيل** (`services/enrolment.py`): رمز موقَّع Ed25519
  بمفتاح مركزٍ مُعمّى عند الراحة، **قصير الأجل (١٥ د)**، **أحادي الاستخدام**، ومربوط
  ببصمة الجهاز عند الاستهلاك، ويفشل مغلقًا في كل مسار (تشويه/توقيع خاطئ/انتهاء/إعادة
  استخدام). ‎+١٧‎ اختبارًا تُزوّر الرموز وتُمدّد صلاحيتها فعلًا.
- **لم يُبنَ الوكيل**، و[03_الوثائق/AGENT_SPIKE.md](03_الوثائق/AGENT_SPIKE.md) يوثّق
  **ما لم يُثبَت** صريحًا: عزل الجلسة 0 (خدمة النظام لا ترسم نوافذ)، وأن **winget
  كـSYSTEM غير مدعوم** (يلزم WU/USO COM)، ودورة حياة الخدمة والتحديث الذاتي، وأن خدمة
  SYSTEM على كل جهاز **سطح هجوم عالي القيمة** يتطلّب توقيعًا معتمدًا وإلا كان انحدارًا
  أمنيًّا صافيًا، **وفجوة معروفة**: مخزن الأرقام المستهلكة في الذاكرة يُفقَد عند إعادة
  التشغيل (اختبار يُثبِّت الفجوة بدل إخفائها).
- **التقدير والقرار:** ٤–٦ أسابيع + اختبار اختراق؛ **يُؤجَّل لما بعد أول إيراد**.
  ونتيجة لازمة الآن: **إيقاف الاستثمار في مسار WinRM بكلمة المرور** (يبقى إرثًا).
- الوثيقة تُدرِج قائمة **ما لا يجوز قوله في التسويق** — أوّلها «يعمل على الأجهزة
  المضمّة لدومين»، فقد اختبرناه حيًّا وفشل.

## [1.9.0] — 2026-07-25

### أُضيف (Added) — حزمة الدليل + الترخيص (أول مسار إيراد فعليّ)
- **حزمة الدليل (Evidence Pack):** سجلّ لحظيّ لجهاز واحد يذكر: البرامج المثبَّتة
  وإصداراتها، الثغرات المنطبقة على **الإصدار المثبَّت تحديدًا** ومعها **نطاق الإصدارات
  الذي جعلها منطبقة**، التحديثات المُثبَّتة، ونتيجة **التحقّق من سلسلة سجلّ التدقيق**
  مع بصمة السلسلة. تصدير `JSON` و`CSV` (بترميز يفتحه Excel العربيّ بلا تشويش).
- **«مبصومة بالهاش» لا «موقَّعة»:** البصمة SHA-256 على محتوى التقرير نفسه، ويُصرّح
  النصّ أنها **ليست توقيعًا رقميًّا** — تُثبِت تطابق النسخة مع الصادرة ولا تُثبِت هوية
  المُصدِر. لا يوجد مفتاح توقيع، وتسمية التجزئة «توقيعًا» هي بالضبط المبالغة التي
  تُسقِط التقرير أمام المُراجِع. (اختبار يمنع تسلّل هذه الصياغة.)
- **نصوص التقرير كُتبت بعناية:** كل جملة فيها إمّا يمكن التحقّق منها من بيانات التقرير
  أو هي **إقرار بحدّ**: التغطية جزئيّة ونسبتها معلنة، وغير المُطابَق يُدرَج بسببه،
  و«لا نتائج» تعني **غياب نطاق يشمل الإصدار** لا خلوًّا من الثغرات، والتقرير **ليس
  شهادة اعتماد** ولا بديلًا عن ماسح مُعتمَد. ولا يُذكَر أي رقم ضابط تنظيميّ (يجب
  التحقّق من نسخة صادرة لا مسوّدة قبل طبع أي ترقيم).
- **ترخيص بمفتاح عامّ (Ed25519):** التطبيق يحمل **المفتاح العامّ فقط**، فيُتحقَّق من
  المفتاح دون اتصال، **ولا يُمكن إصداره** بلا المفتاح الخاصّ (الذي لا يدخل المستودع).
  المفتاح المزوَّر أو المُعدَّل يدويًّا (لترقية الطبقة) يُرفَض — مُثبَّت باختبارات تُزوّر
  فعلًا. والمفتاح المخزَّن **يُعاد التحقّق منه في كل قراءة**، فالقرص لا يُؤتَمن وحده.
  والمنتهي يبقى توقيعه صحيحًا لكنه **لا يمنح صلاحية**.
- **ما يُدفَع مقابله بدقّة:** الفحص والتحديث والمطابقة الدقيقة وعرض النتائج على الشاشة
  **مجانيّة**؛ المدفوع هو **تصدير الحزمة** (يعيد 402 دون ترخيص). و`GET /preview`
  مجانيّ يعرض الأعداد والتغطية وحالة السلسلة بلا تفاصيل الثغرات ولا بصمة.
- كل تفعيل/تصدير/محاولة مفتاح مرفوضة تُسجَّل في سجلّ التدقيق المتسلسل.
- أداة `tools/make_license.py` لإصدار المفاتيح (جانب المورِّد، لا تُشحَن).

### حدود مُعلَنة صراحةً
التحقّق من الترخيص يعمل على جهاز العميل، وأي فحص كهذا يمكن تجاوزه بتعديل الملفّ
التنفيذيّ. الغرض منع المشاركة العابرة وجعل التزوير غير ممكن عمليًّا — لا حماية DRM.
ولا يوجد اتصال بخادم تفعيل (لأن المنتج يَعِد بألّا تخرج البيانات)، وثمنه أن المفتاح
المسروق يبقى صالحًا حتى انتهائه.

## [1.8.0] — 2026-07-25

### أُضيف (Added) — سجلّ تدقيق مُتسلسل بالهاش (أساس حزمة الدليل)
- جدول `audit_events` **يُضاف إليه فقط**: كل سطر يحمل SHA-256 يشمل حقوله **وتجزئة
  السطر السابق**، فتتكوّن سلسلة؛ وتعديل أو حذف أو إعادة ترتيب أي سطر يُبطل كل
  التجزئات بعده ويُكشَف عند التحقّق. `seq` فريد فالفراغ نفسه دليل حذف.
- **الصراحة في حدود الضمان:** السلسلة **تكشف** التلاعب ولا **تمنعه** — من يملك
  الكتابة على الملفّ يمكنه إعادة حساب السلسلة كاملة. قيمتها أن التعديل **الصامت
  الجزئي** (حذف سطر محرِج) صار مستحيل الإخفاء، وهذا ما يسأل عنه المُراجِع.
- **لا تُسجَّل الأسرار إطلاقًا:** كلمات المرور والمفاتيح تُستبدَل بـ`[redacted]`
  قبل التجزئة (استخدام بيانات الاعتماد يُسجَّل، لا البيانات نفسها).
- **نقاط نهاية للقراءة فقط:** `GET /api/audit/events` و`/verify` و`/digest`.
  **لا توجد نقطة نهاية للكتابة أو التعديل** — ووجودها كان سيُبطل الغرض (مُثبَّت باختبار).
- موصول بالعمليات الحقيقية: تثبيت التحديثات (الحدث الأهمّ في الدليل)، وجرد البرامج،
  وتغيير الإعدادات. و**فشل السجلّ لا يُفشل العملية المُدقَّقة** — تحديثٌ يُلغى لأن
  السجلّ فشل أسوأ من تحديثٍ بلا سجلّ (مُثبَّت باختبار).
- **بصمة السلسلة (`digest`)** قيمة قصيرة تُثبِّت التاريخ كلّه؛ نسخها في تقرير يجعل
  استبدال قاعدة البيانات بالكامل مكشوفًا لاحقًا.

### أُصلح (Fixed) — عيب كان سيُفرغ الميزة من قيمتها
- SQLite **يُسقط المنطقة الزمنية** عند قراءة عمود التاريخ (`+00:00` تختفي)، فإعادة
  حساب التجزئة من التاريخ المقروء **لا تطابق أبدًا** — أي أن التحقّق كان سيُبلّغ عن
  «تلاعب» في **كل** سطر سليم، فيصير صياحًا مستمرًّا يُخفي التلاعب الحقيقي. الحلّ:
  تخزين نصّ التاريخ المُهشَّم نفسه في عمود `at_iso` وتجزئة **البايتات المخزَّنة**
  (نفس مبدأ حقل `detail`). اكتُشف بالاختبار قبل الشحن.

## [1.7.1] — 2026-07-25

### أُضيف (Added) — واجهة النتائج الدقيقة
- تبويبان في صفحة الأمان: **«بالمصنّع (إرشاديّ)»** و**«بالإصدار (دقيق)»**، فيرى المستخدم
  الفرق بين مستويَي الدقّة بدل خلطهما.
- التبويب الدقيق يعرض لكل منتج: **اسم CPE المُستعلَم به** (فيمكن إعادة إنتاج النتيجة)،
  و**سبب انطباق كل ثغرة** («حتى 26.02 ضمنًا»)، ودرجة الخطورة، ورابط NVD.
- **الصدق مبنيّ في العرض لا مُلحق به:**
  - **نسبة التغطية معروضة دائمًا** (مثلاً «٢١ من ٤٠ منتجًا قابلة للمطابقة الدقيقة»).
  - **النتائج الفضفاضة مفصولة ومُعلَّمة** — سجلّات NVD بلا حدود إصدار تُطابق كل النسخ،
    فلا تُعرَض كمؤكَّدة (Chrome يُنتج ٣ منها فعليًّا).
  - **غير المُطابَق يُعرَض بسببه** (لا تعريف CPE موثوق / إصدار غير قابل للمقارنة / لم
    يُفحص) — والصمت هنا تضليل.
  - «لا نتائج» يُقال صريحًا إنه **لا يعني الخلوّ** بل غياب نطاق يشمل الإصدار.
  - يُبيَّن قصّ الإصدار عند حدوثه (2.55.0.3 ← 2.55.0) فلا يبدو رقمًا مختلَقًا.
- لا يُتّصل بـNVD إلا بضغطة صريحة. ترجمة كاملة في اللغات الست، و‎+٩‎ اختبارات واجهة
  (المجموع **١٢١** في ١٧ ملفًّا).

## [1.7.0] — 2026-07-25

### أُضيف (Added) — مطابقة ثغرات دقيقة بالمنتج والإصدار (CPE)
- **من «تعرّض إرشاديّ» إلى نتيجة قابلة للتدقيق:** كانت الثغرات تُطابَق بكلمة مفتاحية
  لاسم المصنّع. الآن يُسأل NVD بـ**اسم CPE 2.3 كامل** (`cpe:2.3:a:vendor:product:version`)
  فيقيّم **الإصدار المثبَّت فعلًا** مقابل نطاقات كل تحذير.
- **`GET /api/security/precise`** يعيد لكل منتج: اسم CPE المستخدم، والإصدار بعد
  التطبيع (وقبله)، و**سبب انطباق كل ثغرة** (مثل «يؤثّر على ما قبل 26.03») — فالنتيجة
  يمكن مراجعتها لا مجرّد تصديقها.
- **خريطة CPE مُنسَّقة ومُتحقَّق منها مقابل قاموس NVD** (٢١ منتجًا)، مع **قائمة رفض
  موثَّقة (١٩ منتجًا)** تشرح *لماذا* لا تغطية لكلٍّ منها. مصائد جُنِّبت بالدليل:
  - `microsoft:edge` هو Edge القديم (EdgeHTML) — الصحيح `edge_chromium`.
  - `microsoft:windows_app` هو عميل سطح المكتب البعيد لا Windows App SDK.
  - «Insecure» في `Insecure.Nmap` هو الناشر لا مصنّع CPE (الصحيح `nmap`).
  - Teams وPower BI وOneDrive وClaude: مخطّطات إصدارات **غير قابلة للمقارنة**، فلا
    تُطابَق أصلًا (المطابقة كانت ستُنتج نتائج خاطئة بثقة).
- **فصل «المحدود» عن «الفضفاض»:** بعض سجلّات NVD القديمة تُطابق **كل** إصدار بلا حدود.
  تحقّقنا حيًّا: استعلام Chrome 150 أعاد ٣ ثغرات **كلّها فضفاضة**، وPython 3.14 أعاد
  ثغرة من ٢٠٠٩. تُعرَض هذه في `broad_matches` مُعلَّمة، ولا تُقدَّم كدليل على البناء الحالي.
- **قصّ الإصدار حيث يلزم:** Git for Windows يبلّغ `2.55.0.3` وNVD يتتبّع `2.55.0` —
  وبلا القصّ يُعيد الاستعلام صفرًا فيُقرأ كـ«لا ثغرات».
- **يفشل مغلقًا:** المنتج غير المُخطَّط أو الإصدار غير القابل للمقارنة يُعاد في
  `unmatched` **مع السبب**، ونسبة التغطية تُعلَن — فلا يُوحي التقرير بشمولٍ لا يملكه.

### تحقّق ميدانيّ
على الجرد الحقيقيّ مقابل NVD الحقيقيّ: **7-Zip 26.02** ثغرة محدودة
(`end_including=26.02`) و**Nmap 7.95** ثغرة محدودة (`end_including=7.99`) — دليلان
حقيقيّان؛ وChrome ٣ نتائج فضفاضة مُعلَّمة. ٣٣١ اختبار backend، Black + ruff نظيفان.

## [1.6.0] — 2026-07-25

### أُضيف (Added) — جرد البرامج المثبَّتة فعلًا (أساس مطابقة الثغرات الدقيقة)
- **معرفة ما هو مثبَّت، لا ما هو قديم فقط:** كان التطبيق يخزّن الحزم التي **لها
  تحديث متاح** حصرًا، فلا يعرف المنتجات المثبَّتة ولا إصداراتها — ومطابقة CVE الدقيقة
  (CPE) تحتاج **المنتج والإصدار** معًا، وأي تقرير أصول يحتاج الجرد.
- **جدول `installed_software`** (منتج + إصدار + ناشر + مصدر + حقل `cpe` مُهيَّأ)،
  مُفصَّل لكل جهاز، وأُضيف `device_id` لجدولَي التحديثات مع نقل الفرادة إلى
  `(device_id, package_id)` و`(device_id, kind, update_id)` — فصار **تمثيل أسطول
  أجهزة ممكنًا** بعد أن كان مستحيلًا في المخطّط.
- **نقاط نهاية جديدة:** `GET /api/updates/inventory` و`POST /api/updates/inventory/refresh`.
- **مُحلِّل `winget list` مقاوم للّغة** (منفصل عن محلِّل `upgrade`، لأن الأخير يشترط
  عمود «متوفّر» فكان سيُسقط كل المنتجات المثبَّتة). مُتحقَّق منه على إخراج **حقيقيّ**:
  ١٢٢ صفًّا ← ١١٥ منتجًا فريدًا، **صفر صفوف مرفوضة**.
- **عيبان حقيقيّان اكتُشفا بالتحقّق الميدانيّ وأُصلحا** (وكانا سيُسقطان منتجات بصمت):
  1. تصادم عمودَي الاسم والمعرّف عند طول الاسم (أسقط **Visual C++ Redistributable**).
  2. معرّفات بنمط مسار تحتوي مسافات مشروعة (أسقط **Microsoft 365**).
- **لا حذف عند إخراج منقوص:** إن خرج winget برمز غير صفريّ (احتمال إخراج جزئيّ)
  تُحدَّث الصفوف ولا تُحذف الغائبة — فلا يُمحى جرد حقيقيّ بسبب فحص ناقص.

### ملاحظة
هذه دفعة **أساس** — لا واجهة مستخدم لها بعد؛ تظهر ثمرتها في مطابقة CVE بالإصدار
الدقيق ولوحة الأسطول لاحقًا.

## [1.5.10] — 2026-07-25

### تغيير (Changed) — صدقٌ في تسمية نتائج الأمان
- صفحة الأمان صارت تُعرِّف نفسها **«تعرّض إرشاديّ — ليس تقييمًا للثغرات»** بلافتة تحذير
  ظاهرة دائمًا، وتُصرِّح أن المطابقة **باسم المصنّع لا بالإصدار المُثبَّت الدقيق**، فقد
  تظهر نتائج زائدة أو تغيب نتائج ناقصة، وأنها **لا تُعتمد دليلًا تنظيميًّا أو تدقيقيًّا**
  ولا بديلًا عن ماسح ثغرات مُعتمَد.
- التحذير نفسه يُطبَع الآن **داخل تقرير PDF** فلا يُقتطَع التقرير من سياقه.
- **السبب:** `cve.py` يستعلم NVD بـ`keywordSearch` (اسم المصنّع)، و`software_packages`
  يحتفظ فقط بما له تحديث متاح — أي أن التطبيق يعرف ما هو قديم ولا يعرف ما هو مثبَّت.
  تقديم ذلك كـ«نتائج أمنية» يضلّل المستخدم؛ والتسمية الصريحة تحفظ الثقة إلى أن تصل
  مطابقة CPE بالمنتج+الإصدار.
- ترجمة كاملة في اللغات الست + اختبار يمنع حذف اللافتة (١١٢ اختبار واجهة).

## [1.5.9] — 2026-07-25

### أُصلح (Fixed) — رسالة «فشل المصادقة» كانت تتّهم كلمة المرور وحدها
- خطأ WinRM ‏401 ليس دائمًا كلمة مرور خاطئة: هو **رفض تسجيل دخول شبكيّ (النوع 3)**،
  والجهاز الهدف المُقسّى يرفضه لأسبابٍ تبدو متطابقة من جهة العميل:
  **منع «الوصول إلى هذا الجهاز من الشبكة» للحسابات المحلّية** (أساس التقسية الأمنيّ في
  نسخ Windows Enterprise/أجهزة العمل المُدارة)، أو **تقييد NTLM الوارد**، أو **قفل
  الحساب** بعد محاولات فاشلة. ونجاح RDP لا ينفي ذلك — فهو حقّ دخولٍ مختلف تمامًا.
- الرسالة الآن تذكر هذه الأسباب وتوجّه المستخدم إلى **الحدث 4625** في سجلّ الأمان على
  الجهاز الهدف، حيث السبب الدقيق (‎0xC000015B‎ = منع الدخول الشبكيّ · ‎0xC0000234‎ =
  حساب مقفل · ‎0xC000006A‎ = كلمة مرور خاطئة) — بدل مطاردة كلمة مرور سليمة.
- تُحقِّق من التشخيص ميدانيًّا: الهدف يُصدر تحدّي NTLM صحيحًا (Type‑2) ثم يرفض الدخول،
  ما يُثبت أن الرفض في طبقة السياسة لا في الاعتماد. لا تغيير في منطق الاتصال.

## [1.5.8] — 2026-07-24

### أُضيف (Added) — تغيير كلمة مرور التطبيق من الإعدادات
- قسم **«تغيير كلمة المرور»** جديد في صفحة الإعدادات: يطلب الكلمة الحالية ثم الجديدة
  (مع تأكيدها)، ويستدعي نقطة النهاية القائمة `POST /api/auth/change` التي تتحقّق من
  الكلمة الحالية، وتضبط الجديدة، **وتُنهي بقيّة الجلسات**، وتُصدر رمز جلسة جديدًا
  يُحفَظ تلقائيًّا فلا تُطرَد بعد التغيير.
- سدّ فجوة: كانت الخلفية تدعم التغيير مسبقًا لكن **لا واجهة تستدعيه** إطلاقًا.
- تحقّق من جهة العميل (٦ أحرف على الأقل + تطابق التأكيد) وإظهار أخطاء الخادم (مثل
  «الكلمة الحالية غير صحيحة»). ترجمة كاملة في اللغات الست، و‎+٤‎ اختبارات واجهة
  (إجمالي الواجهة الآن **١٦ ملفًّا / ١١١ اختبارًا**).

## [1.5.7] — 2026-07-24

### أُضيف (Added) — صفحة الإعدادات داخل التطبيق
- **صفحة «الإعدادات»** جديدة (تُفتح من أيقونة الترس في الرأس) تجمع في مكانٍ واحد:
  - **المظهر (الثيم):** اختيار من ٨ ثيمات (تَبع النظام + ٧ ثيمات)، يُطبَّق فورًا.
  - **اللغة:** تبديل بين اللغات الست، يُطبَّق فورًا.
  - **فحص الشبكة:** طريقة الفحص (تلقائي / Python فقط / nmap دائمًا)، تفعيل **الفحص
    التلقائي المجدوَل**، والفاصل الزمني (٥–١٤٤٠ دقيقة) — مع زرّ حفظ صريح.
- **نقطة نهاية `GET/POST /api/system/settings`**: تقرأ وتحفظ إعدادات الفحص في
  `config.json` وتُطبِّقها على الإعدادات الحيّة، وتُعيد جدولة المجدوِل فور تبديله
  (يسري فورًا دون إعادة تشغيل).
- **قائمة سماحٍ صارمة (whitelist):** لا يستطيع الطلب تعديل إلا `scan_method` و
  `scan_scheduler_enabled` و`scan_interval_minutes`؛ ولا يمكن لأيّ طلبٍ كتابة مفاتيح
  حسّاسة (رمز الجلسة، رابط قاعدة البيانات، عبارة التشفير، مفتاح Anthropic).
- ترجمة `settings.*` كاملة في اللغات الست.

### الجودة (Tests) — تغطية اختبارات الواجهة الأماميّة
- إضافة اختبارات واجهة لـ**٨ صفحات** لم تكن مغطّاة: الإعدادات، أندرويد، الأجهزة،
  المنزل الذكي، لينكس، الأمان، التحديثات، وتحديثات البرامج. أصبح إجمالي اختبارات
  الواجهة **١٦ ملفًّا / ١٠٧ اختبارًا** (كانت ٦ ملفات / ٤٧). واختبارات الـbackend
  **٢٨٦** (‎+٧‎ للإعدادات). فحص الأنواع (`tsc --noEmit`) نظيف.

## [1.5.6] — 2026-07-24

### أُصلح (Fixed) — خطأ حرج: الواجهة لا تتحدّث بعد الترقية
- **السبب الجذريّ لعدم ظهور أي ميزة جديدة بعد التثبيت:** نافذة WebView2 تحتفظ بكاش
  HTTP دائم بين التحديثات (`private_mode=False` + `storage_path`)، وكان `index.html`
  يُقدَّم بلا ترويسة `Cache-Control`، فيخزّنه WebView2 استدلاليًّا ويُقدّم **الواجهة
  القديمة** بعد الترقية — بينما يقرأ الـbackend رقم الإصدار من الملف فيظهر الجديد.
  النتيجة: يُثبّت المستخدم نسخةً أحدث لكن لا يرى أي تغيير في الواجهة.
- **الإصلاح (طرفان):**
  1. الخادم يُقدّم الملفات الساكنة بترويسة `Cache-Control: no-cache` (إعادة تحقّق
     رخيصة عبر ETag على المضيف المحليّ) فلا تُقدَّم واجهةٌ قديمة أبدًا.
  2. النافذة تُحمّل الواجهة على رابطٍ موسومٍ بالإصدار `…/?_v=<version>`، فيتغيّر الرابط
     مع كل ترقية ويُجبَر جلب `index.html` الجديد (وملفّات JS الجديدة ذات البصمة).
- **علاجٌ فوريّ للنسخ المُثبَّتة الآن:** حذف مجلّد كاش WebView2 يدويًّا يُظهر الواجهة
  الحقيقيّة دون انتظار: `%LOCALAPPDATA%\HomeUpdater\WebView2`.

## [1.5.5] — 2026-07-24

### تحسين (Changed)
- زرّ **«حل المشكلة»** في صفحة الأمان صار **ظاهرًا دائمًا** (لم يعد مرتبطًا بوجود أجهزة
  مفحوصة)، فلا يختفي قبل الفحص. وإن لم تُفحَص الشبكة بعد، يطلب من الذكاء الاصطناعيّ
  نصائح أمانٍ عامّة للشبكة المنزليّة بدل تحليل ثغراتٍ محدّدة.

## [1.5.4] — 2026-07-24

### أُضيف (Added) — «حل المشكلة» بذكاء اصطناعيّ (محليّ مجانيّ أو سحابيّ مدفوع)
- زرّ **«حل المشكلة»** في صفحة الأمان: يحلّل ثغرات الأجهزة (المصنّع + CVE) ويقترح خطوات المعالجة.
- **طبقة توجيه (router) بعقدٍ موحَّد `AIProvider`** تبدّل بين مزوّدَين، بعزلٍ تامّ وسقوطٍ تلقائيّ:
  - **المحرّك المجانيّ المدمج**: نموذج لغويّ صغير عبر `@mlc-ai/web-llm` (WebGPU) داخل النافذة —
    بلا مفتاح وبلا خادم خارجيّ، ويعمل بلا إنترنت بعد تنزيلٍ واحد للنموذج. **يُحمَّل كسولًا**
    (chunk منفصل ~5.9م.ب لا يُحمَّل إلا عند اختياره) فلا يُثقل الأجهزة التي لا تستخدمه؛ الحزمة
    الرئيسية تبقى ~517ك.ب، والنموذج نفسه غير مُحزَّم في المثبِّت.
  - **ذكاء العميل المدفوع**: يعيد استخدام واجهة المستشار الحاليّة كواجهة خارجية (بلا تعديل لكودها).
- **كشف القدرة + السقوط الآمن**: على جهاز بلا WebGPU يُستخدم السحابيّ تلقائيًّا؛ فشل المحليّ →
  سحابيّ؛ لا هذا ولا ذاك → رسالة واضحة. بثّ تدريجيّ للإجابة، وعزل ذاكرة GPU عند التبديل. ٦ لغات،
  واختبارات موجِّه للتبديل/السقوط.

## [1.5.3] — 2026-07-24

### تجربة المستخدم (UX)
- **رسالة فشل التحديث أوضح + زرّ «إعادة المحاولة»:** بدل تركِ المستخدم أمام رسالة
  عامّة، يعرض صندوق الخطأ الآن تلميحًا وديًّا («قد تكون خدمة التحديث مشغولة مؤقّتًا —
  جرّب مرّة أخرى…») مع التفصيل التقنيّ الحقيقيّ وزرّ **إعادة المحاولة** بنقرة واحدة —
  في تبويبَي «تحديثات Windows» و«البرامج» (بست لغات).

### توضيح (لا تغيير في الكود)
- تأكيدٌ بالدليل أن مسار الفحص بلا **nmap** يعمل: عند غياب nmap يسقط التطبيق تلقائيًّا
  إلى الماسح المكتوب بـPython (اكتشف 27 جهازًا في 6.7ث على شبكة حقيقيّة). استيراد
  `nmap` كسولٌ بالكامل منذ الإصدارات الأخيرة، فلا يتعطّل التطبيق على جهازٍ بلا nmap —
  الأجهزة التي تُظهر «خطأ التحميل» تُشغّل بناءً قديمًا سابقًا لهذا الإصلاح؛ الحلّ ترقيتها.

## [1.5.2] — 2026-07-24

### الأمان — تحديث الاعتماديات (Security / deps)
- ترقية اعتماديات وقت التشغيل المُصابة إلى إصداراتٍ مُصحَّحة بعد تدقيق `pip-audit`
  (كانت 50 ثغرة في 9 حزم): **cryptography 44→48.0.1** (٥ ثغرات)، **Pillow 11→12.3.0**
  (٢٤ ثغرة)، **fastapi 0.115.5→0.116.1 + starlette 0.41.3→0.47.2** (٩ ثغرات)،
  **requests 2.32.3→2.33.0**، **zeroconf 0.136.0→0.149.16**. حزمة الاختبارات كاملةً
  (278) تمرّ بلا انحدار على الإصدارات الجديدة.
- بقيّة الإنذارات (13) مُتجاهَلة **بمراجعة موثّقة** في `security.yml` مع سببٍ لكل مجموعة:
  أدوات تطوير لا تُشحَن (black، pytest)، وحزم RESERVED غير مستوردة بلا إصلاح متاح
  (paramiko، scapy)، وإنذارات starlette خارج نموذج التهديد (واجهة محليّة على 127.0.0.1
  لمستخدمٍ واحد؛ وإصلاحاتها المتبقّية تتطلّب starlette 1.x الذي لا يدعمه أيّ fastapi حاليًّا).

## [1.5.1] — 2026-07-24

### البناء والإصدار (Build / CI)
- **توقيع تلقائيّ لكل إصدار:** خطّ الإصدار صار **يوقّع المثبِّت والتطبيق ذاتيًّا (self‑signed)**
  في CI دائمًا — لم يعد الإصدار يخرج بلا توقيع. يُولَّد شهادة توقيع كود مؤقّتة في وقت
  البناء (SHA‑256 + طابع زمني RFC‑3161)، ويُوقَّع الـexe **قبل** حزمه في المثبِّت ثم
  المثبِّت بعده. متى أُضيف سرّا `SIGNING_PFX_BASE64` + `SIGNING_PFX_PASSWORD`، يوقّع CI
  بالشهادة **الموثوقة** بدلًا من الذاتيّة (يُزيل تحذير SmartScreen). توثيق في README.
- **ترقية إجراءات GitHub Actions إلى زمن تشغيل Node 24** (إنهاء إهمال Node 20):
  `checkout@v4→v5`، `setup-python@v5→v6`، `setup-node@v4→v5`، ونسخة بناء Node
  `20→22` عبر ملفات السير الثلاثة (ci / release / security).

## [1.5.0] — 2026-07-24

### أُصلح (Fixed)
- **تحديث winget «يفشل دائمًا» (السبب الجذري):** كان `succeeded = rc == 0` في
  `software_updates.py` يعتبر أكواد winget الحميدة فشلًا — `0x8A15002B`
  (UPDATE_NOT_APPLICABLE) و`3010/1641` (يتطلّب إعادة تشغيل) — فتُحفَظ الحزمة
  `is_installed=False` وتعود «معلّقة» في كل فحص. أُضيف مُصنِّف يُطبِّع الكود إلى
  DWORD (لأن HRESULT قد يصل موجبًا أو سالبًا) ويُدرِج الأكواد الحميدة في قائمة بيضاء،
  ويكشف `reboot_required`.
- **إخفاء الفشل الجزئي:** ملخّص تبويب «البرامج» كان مثبَّتًا على اللون الأخضر —
  صار يتفرّع إلى تحذير عند `installed != total` (مطابقةً لتبويب Windows).
- **ملاحظة قديمة مضلِّلة:** صفحة التحديثات كانت تصف تحديث Windows البعيد بأنه
  «ميزة مخطَّطة» رغم توفّره فعلًا — صُحِّحت لتشير إلى تبويب «أجهزة Windows البعيدة».

### أُضيف (Added)
- **مساعد الدعم الذكي:** صفحة «الدعم» + `POST /api/advisor/support` — مساعد مقيّد
  بالتطبيق وحده (نصّ نظام مُنسَّق)، بقفل مستقلّ عن المستشار، بلا قاعدة بيانات وبلا
  موافقة T11 (لا تُرسَل بيانات شبكة إطلاقًا). يُعيد استخدام مفتاح المستشار.
- **رسالة إتمام الترقية:** عند الإقلاع بعد أن يستبدل المثبِّت الموقَّع الملفات ويعيد
  التشغيل، تظهر «تم ترقية التطبيق من X إلى Y» مرّة واحدة (خدمة `version_state` +
  `GET /api/system/upgrade-notice` + توست يُكتَم لكل إصدار). **لا** تحديث ذاتيّ
  صامت يشغّل مثبِّتًا مرتفع الصلاحيات — قرار أمنيّ متعمَّد.
- **منتقي أجهزة في صفحة الاتصال البعيد (WinRM):** اختر جهازًا مكتشَفًا فيُملأ العنوان
  والاسم تلقائيًّا؛ اسم المستخدم وكلمة السرّ هما الحقلان الظاهران الوحيدان،
  والمنفذ/HTTPS/الإدخال اليدويّ تحت «خيارات متقدّمة».

### نُظّف (Cleaned)
- إزالة كود ميّت (`DEFAULT_HTTPS_PORT`، `SEVERITY_LEVELS`) و**26 مفتاح i18n يتيمًا**
  عبر اللغات الستّ (بعد تحقّق آليّ من انعدام كل إشارة — حرفيّة أو شرطيّة أو ديناميكيّة).

### الأمان (Security)
- تدقيق سداسي شامل: الوضع الأمنيّ سليم من الطرف للطرف (PBKDF2 + تشفير عند التخزين +
  subprocess بلا shell + بلا أسرار في السجلّات) — يُشحَن كما هو.

### مُختبَر (Tested)
- **322 اختبارًا** (backend 278 + frontend 44). حارسا الأمان — بوّابة موافقة المحادثة
  وبوّابة تأكيد «طبّق» — مُتحقَّق منهما **بالطفرة (mutation testing)**. ruff + tsc نظيفان.

## [1.4.7] — 2026-07-21

### أُصلح (Fixed) — معالجات المنصّة (3 مشاكل)
- **nmap اختياري حقًّا (خطأ التحميل):** كان `import nmap` في **أعلى** `discovery.py`
  يجعل `python-nmap` اعتمادية تحميل صلبة — لو لم تُحزَّم لفشلت الوحدة في التحميل
  وسقط التطبيق بـ «خطأ أثناء تحميل البرنامج». الآن الاستيراد **كسول** داخل مسار
  nmap فقط (مع حارس `TYPE_CHECKING` لتلميحات الأنواع)؛ الماسح الخالص (python) يبقى
  الافتراضي، ولا يُنزَّل ثنائي nmap تلقائيًّا (يناقض الترخيص).
- **adb أكثر متانة (توقّف/خطأ تحميل):** `_run_adb_blocking` كان لا يُثبّت
  `stdin=subprocess.DEVNULL` عند غياب مدخل — والبناء بلا كونسول لا يملك مقبض stdin
  صالحًا، فقد يعلّق adb أو يفشل. أُصلح، وأُضيفت `adb_check()` (فحص توفّر عند الإقلاع)
  و`adb_state()` (حالة اتصال صريحة قبل العمليات).
- **خطأ 740 (ERROR_ELEVATION_REQUIRED):** وحدة `app/win_elevation.py`
  (`ensure_elevated`/`launch_elevated`) — بلا أثر في الـ exe (مانيفست
  `requireAdministrator` يرفعه أصلًا)، تُعيد إطلاق التشغيل من المصدر مرتفعًا، وتُطلق
  المُثبِّت الابن (التحديث الذاتي T4) مرتفعًا؛ دون تعطيل UAC.

### أُضيف (Added) — i18n + a11y + CI
- إكمال ترجمات اللغات الستّ (fr/es/tr/ur — 362 مفتاحًا لكل لغة، بلا fallback صامت).
- تمريرة وصولية (a11y): رابط «تخطٍّ إلى المحتوى» + معلَم `<main>`، `aria-label`
  للأزرار الأيقونية، ربط الحقول بتسمياتها، و`scope`/`caption` للجداول.
- مصفوفة CI: اختبار الـ backend على Python **3.11 و3.12**.

### مُختبَر (Tested)
- 241 اختبار backend (8 جديدة للمعالجات) · بناء الواجهة نظيف · ruff+black نظيف.

## [1.4.6] — 2026-07-21

### أُضيف (Added) — قرارات المنتج + Fast Wins (من تدقيق المشروع)
- **T4 — تحديث ذاتي:** `GET /api/system/update-check` يقارن الإصدار الحالي بأحدث
  إصدار على GitHub (cache ساعة، fail-soft)، وشارة في الواجهة «يتوفّر إصدار أحدث»
  بتنزيل بنقرة. **قرار:** إشعار + رابط فقط — لا تشغيل صامت لمثبّت مرتفع الصلاحيات.
- **T11 — موافقة الـ Advisor:** نصّ موافقة صريح ثنائي اللغة يُبيّن ما يُرسَل إلى
  Anthropic (أسماء الأجهزة/IPs/الموردون/عدّاد CVE/التحديثات المعلّقة) وما لا يُرسَل
  (كلمات المرور/الاعتمادات/الملفات)، وحدود الضمان، وحقوق البيانات؛ مع بوّابة تحجب
  `analyze`/`chat` (403) حتى القبول، وشاشة قبول/سحب في الواجهة.
- **T15 — صدق القدرات:** حقل `manageable` + وسم «غير مُدار مباشرة» للأجهزة التي لا
  يستطيع التطبيق تحديثها (router/TV/IoT/unknown) بدل إيهام المستخدم — دون إخفائها.
- **T7 — مُجدوِل اختياري:** فحص دوري كل `scan_interval_minutes`
  (`scan_scheduler_enabled`، مُطفأ افتراضيًا) مع بوّابة مشتركة تمنع التسابق.
- **T6 — أمن التبعيات:** Dependabot (pip + npm + actions) و`pip-audit` في CI.
- **T10 — حوكمة المستودع:** `LICENSE` (مِلكي/source-available) و`SECURITY.md`
  (سياسة إفصاح عن الثغرات) و`CONTRIBUTING.md`.

### مُختبَر (Tested)
- 233 اختبار backend (10 جديدة) · بناء الواجهة نظيف · ruff+black نظيف.

## [1.4.5] — 2026-07-21

### أُصلح (Fixed) — عنقود الموثوقية الحرج (من تدقيق المشروع)
- **[T1] تزامن SQLite:** تفعيل WAL + `busy_timeout=5000` + `synchronous=NORMAL`
  عبر PRAGMA عند الاتصال. كان المحرِّك على journal الافتراضي بمهلة انشغال صفرية
  مع كُتّاب متزامنين (معالِجات الطلبات، جلسة الفحص الخلفية، تطبيق الـ Advisor،
  كتابة cache الـ CVE)، فأيّ تداخل يرفع `database is locked` → 500 غامض. الآن
  القراءة تتوازى مع الكتابة، والكاتب المحجوب لحظيًا ينتظر ويعيد المحاولة.
- **[T2] تطبيق خطة الـ Advisor في الخلفية:** كان التثبيت الطويل (حتى ساعتين) يُنفَّذ
  متزامنًا داخل طلب `/api/advisor/apply` محتجِزًا الجلسة وخانة التحديث؛ فإغلاق نافذة
  WebView2 أو انقطاع العميل يُسقطه ويترك تثبيتًا نصف مُطبَّق. الآن يعمل كمهمة خلفية
  بجلسة قاعدة بيانات مستقلّة (مع `asyncio.shield`)، فالردّ يبقى كما هو للعميل المتصل،
  لكن إغلاق النافذة لا يُلغي التثبيت ولا يترك الخانة محجوزة.
- **[T3] ضوابط الـ AI Advisor:** مهلة لكل نداء (120s) + مهلة إجمالية للحلقة (300s) +
  `max_retries=2` (تراجع تلقائي على 429/529) + قفل يرفض النداءات المتزامنة — فلا
  إنفاق token جامح من نقرات متكرّرة ولا نداء بطيء يحجب الطلب بلا حدّ.
- **[T5] نسخة احتياطية قبل الترحيل:** يُلتقَط snapshot لقاعدة البيانات قبل أيّ
  `alembic upgrade` (تُحفَظ آخر 3)، فترحيلٌ يفشل في المنتصف قابل للاسترداد.

### مُختبَر (Tested)
- 227 اختبار backend (5 جديدة تغطّي قفل الـ Advisor + الخلفية + النسخ الاحتياطي)
  · ruff+black نظيف.

## [1.4.4] — 2026-07-21

### أُصلح (Fixed) — تدقيق شامل (١٧ خللاً حقيقياً مؤكَّداً)
تدقيق متعدّد الوكلاء (فريق) بمراجعة خصامية على العمل الأخير وكامل التطبيق، ثم
إصلاح كل خللٍ مؤكَّد:
- **[عالٍ] عاصفة NVD (429):** لم يكن التراجع التكيّفي (AIMD) يُطبَّق عند الرفض
  429/403 — كانت الطلبات تتوالى وتُصعّد الحظر. الآن يُباعَد كل طلب شبكي بالمهلة
  الحاليّة (حتى في حالة الرفض).
- **[عالٍ] تعليق SSH اللامحدود:** أوامر SSH بلا مهلة ولا keepalive، فقد تُعلّق
  للأبد وتُعطِّل خانة التحديث المشتركة (409 دائم عبر المستشار). الآن مهلة لكل أمر
  + كشف سقوط الطرف الصامت.
- **[متوسط] تعليق مهمّة المسح الخلفية:** قد يجمعها جامع القمامة فيبقى 409 للأبد.
  الآن مرجع قويّ + استرداد تلقائي يحرّر الخانة إن ماتت المهمّة.
- **[متوسط] هويّة الأجهزة عند الحفظ (٤ مشاكل):** تكرار الجهاز وضياع اسمه عند تدوير
  الـ MAC (خصوصية الجوّالات)، عدم ترقية جهاز رُصد بلا MAC ثم عُرف، تلوّث بيانات
  جهاز عند إعادة DHCP لعنوان IP، وارتداد نوع الجهاز إلى «غير معروف». الآن مطابقة
  ذكيّة بالهويّة تحفظ تسميات المستخدم وملاحظاته.
- **[متوسط] تصلّب الأمان:** قفل بعد محاولات دخول فاشلة، انتهاء صلاحية للجلسات
  (خمول/مطلق + حدّ)، وحجب توثيق الـ API (/docs, /openapi.json) خلف بوّابة كلمة المرور.
- **[متوسط] تثبيت winget:** توسيع نافذة السكون (١٠د) + إنهاء **شجرة** العملية عند
  الإجهاض (لا يبقى المثبِّت يتيماً يعمل بينما يُبلَّغ عن فشل).
- **[متوسط] سقوف WUA + تلوّث التقدّم:** رفع السقوف للأجهزة البطيئة (تثبيت حتى ساعتين)
  + **حارس جيل** يمنع خيطاً مهجوراً من تلويث تغذية تقدّم تشغيلٍ جديد.
- منخفضة: تصحيح تحيّز مُقدِّر مهلة Home Assistant، حدّ لنموّ المقدِّرات في الذاكرة،
  ونقل حفظ الإثبات خارج حلقة الأحداث.

### مُختبَر (Tested)
- 222 اختبار backend (7 جديدة تغطّي مطابقة الهويّة + تصلّب الأمان) · ruff+black نظيف.

## [1.4.3] — 2026-07-20

### أُصلح (Fixed)
- **فشل حفظ نتائج المسح على قواعد البيانات القديمة** (`NOT NULL constraint
  failed: devices.mac`): قواعد البيانات التي أنشأتها إصدارات ما‑قبل‑Alembic جعلت
  عمود `devices.mac` **غير قابل لأن يكون فارغاً (NOT NULL)**، بينما تصميم الأجهزة
  بلا عنوان MAC يُدرج `mac=NULL` (يسمح SQLite بعدّة قيم NULL في عمود UNIQUE، وهذا
  أساس التصميم). كان مسار تبنّي Alembic يكتفي بـ«ختم» الأساس (baseline) دون إعادة
  بناء الجدول، فيبقى القيد القديم، فيفشل حفظ أيّ جهاز بلا MAC (مثل جهاز المستخدم
  نفسه). الآن **هجرة جديدة تُصلح العمود إلى nullable مع الحفاظ التامّ على كل
  الأجهزة والملاحظات** (إعادة بناء صريحة للجدول: إعادة تسمية → إنشاء → نسخ → حذف →
  إعادة الفهارس؛ مع حارس يجعلها بلا أثر على القواعد السليمة). تحقّقٌ حيّ: قاعدة
  مستخدم فعلية شُفِيت وحُفظت أجهزتها الـ21 كاملةً.
- **عزل الاختبارات:** كانت الاختبارات تقرأ مجلّد بيانات النظام الحقيقي، فإذا كان
  المستخدم قد ضبط كلمة مرور للتطبيق (وُجد `auth.json`) تُرفض النقاط المحميّة بـ401.
  الآن تُعزَل الاختبارات في مجلّد بيانات مؤقّت (215 اختباراً يمرّ محلّياً كما في CI).

## [1.4.2] — 2026-07-20

### أُضيف (Added) — مهلات شبكة تكيّفية (Adaptive network timeouts)
مشروع كامل صمّمه الفريق (مُدقَّق ومُنفَّذ) يحلّ محلّ المهل الزمنية الثابتة المبثوثة
في التطبيق بآلية تتعلّم زمن الشبكة وتتكيّف معه، فتقلّل الإخفاقات الكاذبة على
الوصلات البطيئة وتُحكِم العمليات المفتوحة الطول:
- **الأساس:** `AdaptiveNetworkTimeout` (RTO بأسلوب Jacobson‑Karels، RFC 6298،
  بلا قسمة على صفر) لكل probe/connect؛ `StallWatchdog` (إجهاض عند توقّف التقدّم)
  للعمليات المفتوحة؛ `DurationCeiling` (سقف تكيّفي للعمليات المُعتِمة)؛
  و`backoff_with_jitter` (full jitter) بين المحاولات.
- **فحص الشبكة (Python):** مهلة الجسّ صارت RTO مُتعلَّماً لكل شبكة (CIDR) يُسرّع
  انتهاء العناوين الميتة على LAN السريعة، مع تمريرة ثانية موسّعة (≥1.5s مع jitter)
  **تصون التقاط الهواتف النائمة** (DTIM).
- **nmap:** ميزانية زمنية مشتقّة من عدد المضيفين (بدل 600s ثابتة لكل الشبكات) +
  حدّ على انتظار الفحص يمنع تعليقاً لا نهائياً.
- **winget:** الإنهاء عند توقّف الخرج (stall‑watchdog) بدل مهلة جدارية ثابتة —
  لا يُقطَع تثبيت بطيء لكنه حيّ.
- **SSH / WinRM / Home Assistant / NVD:** مهلات لكل نقطة/عملية؛ NVD بـ **AIMD**
  يحترم `Retry‑After` ويتراجع عند 429/403 ثم يعود للحدّ الآمن.
- **إثبات (persistence):** تُحفَظ التقديرات على القرص فتُدفّأ عبر إعادة التشغيل
  (best‑effort، متسامح مع التلف؛ يُضبَط بـ `adaptive_timeout_persistence`).

### أُصلح (Fixed)
- **تعليق Windows Update اللامحدود:** كانت استدعاءات WUA COM (البحث/التثبيت)
  تُنفَّذ **بلا حدّ زمني**، فقد تُعلّق للأبد عند تعطّل خدمة التحديث **وتُبقي حالة
  التقدّم عالقة** فيرجع كل طلب تحديث لاحق بالرمز 409. الآن مُقيَّدة بسقف تكيّفي عبر
  `asyncio.wait` (لا يحجب على خيط COM غير القابل للإلغاء). *(كشفه الفريق أثناء
  خرائط الكود، وتحقّقتُ منه مباشرةً.)*

### مُختبَر (Tested)
- 213 اختبار backend (≈40 جديدة للمهلات التكيّفية) · ruff+black نظيف · تحقّق حيّ
  للمسح على شبكة حقيقية (طلب فوري، خلفية، 409 للمتزامن).

## [1.4.1] — 2026-07-20

### أُصلح (Fixed)
- **فشل فحص الشبكة على الشبكات المزدحمة**: كان الفحص يُنفَّذ **متزامناً** ويحجب طلب
  POST لأكثر من ١٠٠ ثانية على شبكة كبيرة (أو مع nmap مثبّتاً)، فتنتهي مهلة الطلب في
  المتصفّح ويظهر **«فشل الفحص»** رغم أن الأجهزة وُجدت فعلاً — والعدّاد يبقى صفراً.
  الآن الفحص يعمل **في الخلفية**: يعود الطلب فوراً (٠٫٠٢–٠٫٠٩ ثانية)، والواجهة
  تستطلع `/scan/status` للتقدّم الحيّ حتى الاكتمال ثم تُحدّث قائمة الأجهزة. النتائج
  تُحفَظ في جلسة قاعدة بيانات مستقلّة، ولا يُعلَن «اكتمل» إلا بعد الحفظ فعلاً.
  (تحقّق حيّ على شبكة حقيقية: الطلب فوري، الأجهزة تظهر تدريجياً حتى الاكتمال.)

### مُختبَر (Tested)
- 174 اختباراً backend (منطق الحفظ مُختبَر مباشرةً + بدء الفحص في الخلفية + رفض
  الفحص المتزامن 409). بناء الواجهة نظيف.

## [1.4.0] — 2026-07-20

### أُضيف (Added)
- **قفل الدخول بكلمة مرور (حماية بيانات التطبيق)**: التطبيق يحمل بيانات حسّاسة
  (جرد الأجهزة، اعتمادات SSH/WinRM/HA المشفّرة، مفتاح Anthropic)، فأُضيفت طبقة
  **مصادقة المستخدم** فوق حماية الحلقة المحلية (توكن الجلسة + CSRF):
  - **إعداد أوّل مرّة (بلا كلمة مرور افتراضية)**: يفرض التطبيق على المستخدم إنشاء
    كلمة مروره الخاصة قبل أي استخدام — تجنّباً لثغرة «الاعتماد الافتراضي» (مثل
    `admin`) التي يحذّر منها البرنامج نفسه في صفحة الأمان.
  - كلمة المرور تُخزَّن **hashed فقط** (PBKDF2‑HMAC‑SHA256، 200 ألف تكرار، مِلح
    عشوائي) في `auth.json` بمجلد البيانات — لا نصّ صريح إطلاقاً.
  - تسجيل دخول عند كل تشغيل (الجلسات في الذاكرة)، وتغيير كلمة المرور، وبوّابة
    middleware تحجب كل مسار `/api/*` حسّاس حتى تسجيل الدخول. **opt‑in**: قبل تعيين
    كلمة مرور يعمل التطبيق كالسابق (لا يكسر شيئاً).
  - واجهتا إنشاء/دخول جديدتان، مُدوّلتان بالكامل (٦ لغات).
  - **مراجعة عدائية** لبوّابة المصادقة: لا اختراق للبوّابة ولا قفل دائم؛ نتيجتان
    منخفضتان أُصلحتا (رسالة خطأ ودّية عند تعذّر الحفظ، والتحقّق من التوكن قبل عرض
    التطبيق لمنع وميض الواجهة).

### مُختبَر (Tested)
- **174 اختباراً** backend (بينها تجزئة كلمة المرور والبوّابة). تحقّق حيّ من
  التدفّق: شاشة الإعداد → إنشاء → فتح التطبيق.

## [1.3.4] — 2026-07-20

### أُصلح (Fixed)
- **قصّ الشعار في الشريط العلوي**: كان الشعار/العنوان في أعلى اليمين يُقصّ ويخرج
  خارج حافة النافذة، لأن الرأس كان محدوداً بعرض `max-w-6xl` لا يتّسع لعناصر التنقّل
  التسعة + الشعار + الأزرار. الآن الرأس يستخدم عرض النافذة الكامل (حتى 1800px)، وقائمة
  التنقّل تنساب أفقياً بلا شريط تمرير ظاهر — فلا يُقصّ الشعار ولا الأزرار على أي عرض.
  (ظهرت في تطبيق المستخدم الفعلي بثيم داكن.)

## [1.3.3] — 2026-07-20

### أُصلح (Fixed)
- **تدويل صفحة Android بالكامل (i18n)**: نافذة «إضافة هاتف» — وخصوصاً قسم الإقران
  الجديد وزرّ «اكتشف المنفذ» — كانت نصوصها عربية ثابتة. وتبيّن أن **مساحة أسماء
  Android كلّها كانت مفقودة في ٤ لغات** (الفرنسية/الإسبانية/التركية/الأردية) فتظهر
  بالإنجليزية. الآن كل النصوص عبر `t()` مع ترجمات كاملة في **اللغات الست**.
  (وُجد أثناء تدقيق شامل للمشروع «كمدير»؛ الجوهر — الاختبارات، الأمان، الإصدار — كان
  سليماً بالكامل.)

### مُختبَر (Tested)
- 167 اختبار backend · بناء الواجهة نظيف · مفاتيح i18n متسقة عبر اللغات الست.

## [1.3.2] — 2026-07-20

### أُضيف (Added)
- **اكتشاف منفذ الاتصال تلقائياً (Android لاسلكي)**: منفذ «التصحيح اللاسلكي» عشوائي
  ويتغيّر كل مرّة، فكان المستخدم يحتار أيّ منفذ يُدخل بعد الإقران. الآن:
  - بعد نجاح **الإقران**، يكتشف البرنامج منفذ الاتصال تلقائياً (عبر mDNS من adb)
    ويملأ الخانة — تضغط «إضافة هاتف» فقط.
  - وزرّ **«اكتشف المنفذ»** بجانب خانة المنفذ يعيد الكشف في أي وقت.
  - نقطة نهاية `POST /api/android/discover` تُرجع منفذ `_adb-tls-connect._tcp`
    المُطابق لعنوان الجهاز.

### مُختبَر (Tested)
- **167 اختباراً** ناجحاً (ruff + black نظيفان)، مع تحليل مخرجات `adb mdns services`.

## [1.3.1] — 2026-07-20

### أُضيف (Added)
- **#39 — دعم التصحيح اللاسلكي في Android 11+ (إقران)**: كان ربط الجوّالات الحديثة
  يفشل برسالة `Unknown command: STLS` لأن مكتبة ADB بلغة Python الخالصة لا تُنفّذ
  مصافحة **ADB-over-TLS + الإقران**. الآن نُضمّن أداة **adb الرسمية** من Google
  (`vendor/platform-tools`) ونُشغّلها كعملية فرعية — فتدعم الإقران، والاتصال اللاسلكي
  المشفّر، والمنفذ القديم 5555، كلّها معاً.
  - نقطة نهاية جديدة `POST /api/android/pair`، وقسم **إقران** في نافذة «إضافة هاتف»
    (منفذ الإقران + رمز ٦ أرقام)، مع تصحيح التعليمات المضلّلة.
  - التحقّق من المدخلات محكم: العنوان (لا يبدأ بـ`-`)، الرمز (٦ أرقام)، اسم الحزمة —
    والاستدعاء عبر قائمة وسائط (بلا صدفة).
  - **مراجعة عدائية** (المسار يُشغّل ثنائياً بمدخلات المستخدم): لا حقن؛ أُصلحت ٣ نتائج
    منخفضة (تحديد سقف/مهلة لجلب إصدارات التطبيقات، توحيد الاستثناءات، تنظيف ضجيج adb).
- **صفحة الأمان — رقم CVE ورابط مباشر**: يعرض كل جهاز الآن **رقم أخطر ثغرة (CVE)**،
  والرابط يفتح تلك الثغرة مباشرةً على NVD بدل البحث العام بالمصنّع.

### مُختبَر (Tested)
- **164 اختباراً** ناجحاً (ruff + black نظيفان). تغطية جديدة لتحليل مخرجات adb
  والتحقّق من المدخلات.

## [1.3.0] — 2026-07-20

### أُضيف (Added)
- **#36 — محادثة مع المستشار (AI Chat)**: تبويب محادثة تفاعلي في صفحة المستشار
  يجيب عن أسئلة الشبكة بالأدوات **للقراءة فقط** (لا `set_plan` في المحادثة)، مع
  فقاعات رسائل وسجلّ يُقصّ لآخر ٢٠ دوراً. Endpoint: `POST /api/advisor/chat`.
- **#37 — تقرير أمان PDF**: زرّ «تصدير تقرير» يُصدر توصيات المستشار كملفّ PDF
  عبر الطباعة من إطار مخفي (بلا اعتماديات خادم إضافية).
- **#38 — توسيع «طبّق» للأجهزة البعيدة (WinRM/SSH)**: خطّة المستشار تقدر الآن
  تستهدف الأجهزة البعيدة المُهيّأة، و`apply_plan` يُرقّيها عبر نفس خدمات
  `winrm`/`ssh` التي تستخدمها صفحات الأجهزة. أداة قراءة جديدة `list_remote_targets`
  تتيح للنموذج الاستشهاد بمُعرّفات المضيفين؛ **كل مُعرّف يُتحقَّق منه مقابل مضيف
  مُهيّأ قبل تنفيذ أيّ أمر**.
  - **مراجعة عدائية** (المسار ينفّذ ترقيات فعلية عن بُعد): بُعد الأمان لم يجد اختراقاً
    للمضيفين؛ أُصلحت ٣ نتائج مؤكَّدة:
    - **(عالية) عمر القفل**: المُثبّتات المحلية تستدعي `update_progress.finish()`
      داخلياً فتُحرّر قفل التحديث في منتصف الطلب. صار الطور البعيد (قد يستغرق دقائق)
      يعمل **أولاً** بينما لا يزال القفل مملوكاً فعلاً، فلا يتعارض مع صفحة التحديثات.
    - **(منخفضة)** `isascii()` قبل `int()` — `str.isdigit()` يقبل أرقاماً يونيكودية
      مثل `²` يرفضها `int()` (كان يسبّب `500`).
    - **(منخفضة)** المُعرّفات البعيدة المشوّهة تُسجَّل في `skipped` بدل إسقاطها بصمت.
- **#35 — إصلاح فحص الجوّالات**: رفع مهلة الجسّ + تمريرة ثانية للعناوين التي لم
  تردّ (أجهزة موفّرة للطاقة)، وإعادة ترتيب المُصنّف ليصنّف جوّالات هواوي كجوّالات لا
  كموجّه. اختبار مُعامَل يقفل الانحدار (١٣ حالة).

### مُختبَر (Tested)
- **139 اختباراً** ناجحاً (ruff + black نظيفان). تغطية جديدة: حلقة المحادثة،
  «طبّق» عن بُعد للمضيفين المُهيّأين فقط، ورفض المُعرّفات المشوّهة بلا `500`.

## [1.2.0] — 2026-07-20

### أُضيف (Added)
- **زرّ «طبّق أهمّ ٣ تحديثات» في المستشار الذكي**: المستشار الآن لا يوصي فقط بل
  **ينفّذ** — يُصدر خطّة مُهيكلة عبر أداة `set_plan`، وبضغطة (مع تأكيد) يُطبّق أهمّ ٣
  تحديثات عبر منظومة التثبيت الحالية (`install_many`/`install_updates`).
  - **بوّابة أمان**: كل مُعرّف يُتحقَّق منه مقابل جدول التحديثات المعلّقة قبل التثبيت،
    فلا يمكن للمستشار تشغيل إلا ما هو معلّق فعلاً على الجهاز (نفس ما تُثبّته صفحة
    التحديثات) — لا تثبيت اعتباطي.
  - يشارك قفل التحديث المفرد (لا يتعارض مع صفحة التحديثات)، ويُحدّث حالة `is_installed`
    بعد النجاح، ويُبلّغ بعدد ما نجح فعلاً.
  - **مراجعة عدائية** (بُعد الأمان لم يجد شيئاً؛ نتيجتان متوسطتان أُصلحتا: تثبيت الحالة
    في القاعدة + تصفير نتيجة التطبيق عند إعادة التحليل). 123 اختباراً.

## [1.1.0] — 2026-07-20

### أُضيف (Added)
- **#34 — المستشار الذكي (AI Advisor، ميزة agentic)**: مستشار يعمل بالذكاء الاصطناعي
  عبر Claude (`claude-opus-4-8`) بأسلوب **agentic tool-use** — يستدعي أدوات محلية
  للقراءة فقط (قائمة الأجهزة، الثغرات المعروفة، التحديثات المعلّقة)، ثم يوصي بما
  تُحدِّثه أولاً ولماذا، مرتّباً حسب الخطورة، بلغة طبيعية (عربي/إنجليزي).
  - backend: `app/services/advisor.py` (حلقة أدوات + adaptive thinking) +
    `app/routers/advisor.py` (`/api/advisor/status`, `/api/advisor/analyze`).
  - frontend: صفحة «المستشار الذكي» + تبويب في الشريط.
  - يحتاج مفتاح `HOMEUPDATER_ANTHROPIC_API_KEY`؛ بدونه تُعرض رسالة إرشادية.
  - لا تُرسَل بيانات الأجهزة للسحابة إلا الملخّصات المضغوطة التي يطلبها النموذج.
  - 3 اختبارات (بمحاكاة Claude) تتحقّق من الحلقة والبوّابة.

## [1.0.0] — 2026-07-20

الإصدار الأول للاستخدام: نافذة تطبيق أصلية، تحديث كل أنواع الأجهزة من مكان واحد
(Windows محلي/بعيد، Android، لينكس، المنزل الذكي)، تشفير الاعتمادات، مصادقة الـ API،
تحقّق هوية المضيف، توقيع الكود، وواجهة بستّ لغات. أُزيلت شارة «وضع الاختبار».

### تغيير (Changed)
- **إصدار v1**: `build_mode="release"` (لا شارة اختبار)، ورقم الإصدار `1.0.0`.
- **#32 — تدويل الصفحات الجديدة (i18n، 6 لغات)**: صفحات الأمان والمنزل الذكي
  ولينكس و«Windows بعيد» + تبويباتها كانت نصوصاً عربية ثابتة لا تحترم مبدّل اللغات.
  استُخرجت ~90 سلسلة إلى مساحة مفاتيح `pages.*`، واستُخدم `t()` في المكوّنات الأربعة.
  الترجمات الكاملة بالعربية والإنجليزية، والفرنسية/الإسبانية/التركية/الأردية (وُلِّدت
  عبر workflow ودُقِّقت: اكتمال المفاتيح، حفظ العناصر النائبة {{count}}/{{name}}،
  وعدم ترجمة المصطلحات التقنية NVD/WinRM/winget/TLS/TOFU…). الآن تحترم كل الصفحات
  مبدّل اللغات الستّ.
- **#30 — إعادة تصميم التشغيل التلقائي / وضع الخدمة**: كان اختصار Startup يُطلق
  التطبيق المرفوع الصلاحيات عند الدخول فيفشل/يطلب UAC، ووضع الخدمة كـ LocalSystem
  يفصل البيانات ويكسر فكّ تشفير الاعتمادات. الآن:
  - **مهمّة مجدولة عند الدخول** بأعلى الامتيازات (بلا UAC، بحساب المستخدم فتُشارك
    البيانات والمفتاح) تحلّ محلّ اختصار Startup — خيار في المثبِّت، ويُزال عند الحذف.
  - `HOMEUPDATER_DATA_DIR` يوحّد مخزن البيانات بين الواجهة وأي خدمة (يحلّ انقسام
    البيانات).
  - `service.py`: فرع مُرسِل SCM للحالة المُجمَّدة، + اختيار منفذ حرّ + تسجيل أخطاء
    Event Log. دليل [SERVICE.md](00_دليل_المشروع/SERVICE.md) بالتوصية والمقارنة والحدود.

### الأمان (Security)
- **#31 — تحقّق هوية المضيف (SSH TOFU + WinRM TLS)**: كان SSH يستخدم
  `known_hosts=None` (بلا تحقّق مفتاح — عرضة لـ MITM على الشبكة). الآن **ثقة عند
  أول استخدام (TOFU)**: يُلتقط مفتاح مضيف SSH عند أول اتصال ويُخزَّن ويُتحقَّق منه
  في كل اتصال لاحق (قبل إرسال كلمة المرور)؛ تغيُّر المفتاح يُرفض كاحتمال MITM.
  مُتحقَّق حيّاً مقابل خادم asyncssh حقيقي (المفتاح الصحيح يتّصل، والخاطئ يُرفض).
  وWinRM أضيف خيار **التحقّق من شهادة TLS** (`verify_tls`) لمسار HTTPS بدل تجاهلها
  دائماً — مربّع اختيار في صفحة «Windows بعيد». migration `2531d17a2b88` + اختبارات.
- **#29 — مصادقة الـ API المحلي (منع تصعيد الصلاحيات)**: الـ API المحلي يعمل
  بصلاحيات مسؤول وأي عملية/مستخدم محلي آخر كان يستطيع استدعاء التثبيت/إعادة التشغيل.
  الآن **رمز جلسة عشوائي لكل تشغيل** (`secrets.token_urlsafe(32)`) يُسلَّم للواجهة عبر
  **جزء الرابط (`#t=`)** — لا يُرسَل للخادم ولا في Referer ولا يُسجَّل — تقرؤه الواجهة
  وتمحوه من الرابط وترسله في ترويسة `X-HomeUpdater-Token`. الخادم يُلزمه على كل
  `/api/*` و`/docs`/`/openapi.json` (عدا liveness). مُتحقَّق حيّاً وعبر المتصفّح
  (بلا رمز → 401، بالرمز → 200)، ثم **فحص عدائي (red-team)** بثلاث زوايا: لا تجاوز
  ولا تسريب للرمز؛ أُصلحت ملاحظتان طفيفتان (تسريب معلومات عبر `/api` الجذر، وتوسيع
  نطاق اختيار المنفذ). وضع الخدمة/التطوير غير مُلزَم عمداً (خارج النطاق، مُتابَع).
- **O.5 — تشفير بيانات الاعتماد عند التخزين**: كلمات مرور SSH وWinRM ورمز Home
  Assistant تُشفَّر الآن بـ **Fernet** (AES-128-CBC + HMAC) قبل كتابتها في SQLite،
  وتُفكّ شفافياً عند القراءة عبر نوع عمود `EncryptedString` — فلا يُغيَّر مخطط
  القاعدة (لا migration). المفتاح: من `HOMEUPDATER_SECRET_KEY`، أو مشتقّ من
  `encryption_passphrase` (PBKDF2)، أو ملفّ مفتاح مربوط بمستخدم Windows عبر
  **DPAPI** (نسخ ملفّ القاعدة أو نسخة احتياطية سحابية لم يعُد يكشف الاعتمادات).
  الصفوف القديمة (نصّ صريح) تُقرأ كما هي وتُشفَّر عند أوّل كتابة. `crypto.py` + 5
  اختبارات (منها تحقّق أن العمود يخزّن نصّاً مشفَّراً). يعالج ملاحظة المراجعة العدائية.

### أُضيف (Added)
- **نافذة تطبيق أصلية (WebView2) بدل المتصفّح**: نقطة دخول جديدة `app_window.py`
  تعرض الواجهة في نافذة تطبيق حقيقية عبر WebView2/pywebview — لا متصفّح ولا شريط
  عنوان — بينما يعمل الخادم في خيط خلفي (حلقة رسائل واحدة على الخيط الرئيسي).
  حارس نسخة-واحدة (mutex)، فحص توفّر WebView2 في السجلّ مع **بديل تلقائي للمتصفّح**
  إن غاب، صفحة تحميل مُعلَّمة، وإغلاق النافذة يُغلق الخادم بنظافة. `tray.py` يبقى
  بديلاً. صُمِّمت المعمارية عبر لجنة تصميم (3 مقترحات + قاضٍ) واختير حلّ النافذة
  الواحدة لأقلّ نقاط فشل. مُختبَرة حيّاً: نافذة WebView2 تُفتح وتُحمّل الواجهة وتُغلق بنظافة.
- **المرحلة 1.6 — تحديث Windows عن بُعد (WinRM)**: صفحة «Windows بعيد» تضيف جهاز
  Windows آخر على الشبكة (يُتحقَّق ويُكتشف نظامه قبل الحفظ، كلمة المرور لا تُعرَض)،
  وتفحص وتُرقّي برامجه عبر `winget` بنقرة — كل ذلك عن بُعد عبر WinRM (`pywinrm`،
  transport `ntlm` مُشفَّر حتى على HTTP). خدمة `winrm_hosts.py` (نداءات مُغلَّفة بـ
  `asyncio.to_thread`) + endpoints `/api/winrm/*` + جدول + migration `60df94c429f7`
  + 14 اختباراً. يعالج ملاحظة «لا يمكن تحديث سوى الجهاز المحلي» بسدّ الفجوة فعلياً.
- **توقيع الكود (Code Signing)**: `installer/sign.ps1` — خطّ توقيع كامل يُنشئ شهادة
  موقَّعة ذاتياً (5 سنوات) ويوقّع الـ exe والمثبِّت بـ SHA-256 + طابع زمني RFC-3161
  من DigiCert. يبحث عن `signtool` في Windows Kits تلقائياً (بديل
  `Set-AuthenticodeSignature`)، ويدعم شهادة حقيقية عبر `-PfxPath`/`-Thumbprint`.
  الـ exe والمثبِّت الحاليان **موقَّعان فعلاً**. دليل [SIGNING.md](02_التطوير/installer/SIGNING.md)
  يشرح الحدود بصدق: توقيع ذاتي يزيل التحذير على جهازك بعد استيراد الشهادة، لكنه لا
  يُرضي Smart App Control (يلزم شهادة **EV** من هيئة تصديق للتوزيع). `.gitignore`
  يمنع رفع مفاتيح `.pfx/.p12/.snk/.key`.
- **صفحة الأمان — فحص يدوي لأي مصنّع**: إدخال يكتب فيه المستخدم أي مصنّع/منتج
  (TP-Link، Cisco…) ويفحص ثغراته مباشرة عبر `/api/security/cves` — يعمل **دائماً**
  دون حاجة لمسح الشبكة. يعالج ملاحظة أن زر «فحص الثغرات» كان يظهر معطّلاً (لأنه
  يتطلّب أجهزة بمصنّع معروف)؛ أُضيف أيضاً tooltip يوضّح سبب التعطيل.

### تحسينات الواجهة (Improved)
- **نافذة إضافة هاتف Android**: تعليمات واضحة عن مكان إيجاد الـ IP والمنفذ —
  Android 11+ (Wireless debugging يعرض «IP address & Port» بمنفذ متغيّر، ليس 5555)
  وطريقة USB القديمة (`adb tcpip 5555`)، مع تنبيه الإقران وقبول تصحيح USB.
- **صفحة تحديثات Windows**: ملاحظة صريحة أن تحديثات Windows/البرامج/التعريفات
  تُطبَّق على **الجهاز المحلي (الـ hub)** لأن أدواتها تنفَّذ محلياً، مع توجيه لتبويبات
  الأجهزة الأخرى (Android/لينكس/المنزل الذكي) وذكر أن تحديث أجهزة Windows البعيدة
  يتطلّب WinRM (المرحلة 1.6). وثِّقت الفجوة وتصميم المرحلة 1.6 في
  [DEVICES.md](00_دليل_المشروع/DEVICES.md).

### الأمان (Security)
- إضافة middleware حماية في `backend/app/main.py`: قائمة Host بيضاء (`allowed_hosts`)
  تمنع هجمات DNS-rebinding، وإلزام ترويسة `X-HomeUpdater` على الطلبات المُغيِّرة
  (POST/PUT/PATCH/DELETE) لمنع CSRF عبر إجبار CORS preflight. الواجهة تُرسل الترويسة تلقائياً.
- إضافة تحقُّق صارم من اسم حزمة Android قبل تمريره لأمر `am start` (يمنع حقن أوامر ADB).

### التصحيحات (Fixed)
- **مراجعة شاملة عدائية (21 مشكلة مؤكَّدة، أُصلح 13)**: سباق TOCTOU في «تثبيت الكل»
  (حجز ذرّي لفتحة العملية)، انحراف migration عند تبنّي قاعدة قديمة (كانت تُسقط جداول
  cve/ha/ssh/winrm — الآن stamp للأساس ثم upgrade)، إضافة manifest صلاحيات المسؤول
  (uac_admin) لأن التحديث يحتاجها، تحصين مفتاح التشفير (لا يُعيد نصّاً مشفَّراً كنصّ
  صريح عند مفتاح خاطئ)، حدّ لـ communicate بعد مهلة winget (منع تعليق is_running)،
  علامة «فحص منقوص» تمنع تعليم حزم معلَّقة كمثبَّتة، منفّذ خيوط مستقلّ لـ reverse-DNS،
  find_free_port + تسجيل أخطاء للخدمة، وإصلاحات واجهة (بيانات قديمة في الأجهزة،
  خطأ/نجاح متزامنان في HA، تعطيل أزرار لكل مضيف، عنوان خطأ الفحص). المتبقّي (8) مُتابَع
  كمهامّ تصميم: مصادقة الـ API المحلي، وضع الخدمة، تحقّق هوية المضيف، وi18n الكامل.
- **«أخطاء في التحميل» / فشل الإقلاع عند انشغال المنفذ**: كان التطبيق يثبّت المنفذ
  8000، فإن كان مشغولاً (نسخة قديمة عالقة أو برنامج آخر) يبدأ الخادم ثم يُغلق فوراً
  («فشل التحميل»). الآن `find_free_port` ينتقل تلقائياً إلى 8001/8002/… ويستخدمه
  للنافذة والخادم. وجُعل فحص Host في middleware الأمان **يتجاهل المنفذ** (يتحقّق من
  اسم المضيف loopback فقط) فلا تُرفض الطلبات على المنفذ الجديد. مُختبَر حيّاً: 8000
  و8001 مشغولان → اختار 8002 وفتحت النافذة. (`app_window.py`/`tray.py`/`main.run`.)
- **تعطُّل المسح**: العمود `devices.mac` أصبح `nullable` (NULL بدل `""`)، فلم يعُد جهازان
  بلا MAC يُسقطان المسح بخطأ UNIQUE (شائع بلا صلاحيات مدير). *(يتطلَّب حذف قاعدة البيانات التجريبية مرَّة واحدة.)*
- **تحليل winget على Windows العربي**: أُعيد كتابة `_parse_winget_table` ليقسِّم الصفوف
  على مسافتين+ ويُثبِّت الأعمدة من اليمين بدل الاعتماد على عناوين إنجليزية — فلم يعُد الفحص
  يُرجع صفراً ثمَّ يُعلِّم كل البرامج «مثبَّتة» خطأً.
- **سباق مؤشِّرات التقدُّم**: إضافة قفل حول سجلّ الأحداث في `progress.py` و`update_progress.py`
  لمنع أخطاء 500 المتقطِّعة أثناء استطلاع الواجهة.
- **تسريب COM**: موازنة `CoInitialize`/`CoUninitialize` في `windows_updates.py`.
- **KeyError كامن**: إضافة مفتاح `total` في مسار التثبيت الفارغ.
- **رسائل الأخطاء**: `apiFetch` يقرأ الآن `detail` (شكل HTTPException) إضافةً إلى `error`.
- **تصادم كاش**: تبويبا تحديثات Windows والتعريفات لم يعودا يتشاركان مفتاح كاش واحد.
- **بناء الإنتاج**: إزالة استيراد `cn` غير المستخدَم الذي كان يُفشل `npm run build`.

### أُضيف (Added)
- **تكامل Linux/SSH** — إغلاق آخر فجوة «تنفيذ» كبيرة للأجهزة غير المُدارة بـ HA: صفحة
  «لينكس» تضيف جهاز لينكس عبر SSH (يُتحقَّق عند الحفظ، كلمة المرور لا تُعرَض)، تكتشف
  النظام ومدير الحزم، تعرض الحزم القابلة للترقية (`apt`/`dnf`)، وتُطبّق الترقية بنقرة
  (`sudo -S` عبر stdin). خدمة asyncssh + endpoints + جدول + 7 اختبارات. مُختبَر حيّاً
  عبر بروتوكول SSH حقيقي (خادم asyncssh وهمي: كشف Ubuntu 22.04 + 3 حزم + رفض كلمة مرور خاطئة).
- **تكامل Home Assistant** — قفزة نحو «كل الأجهزة من مكان واحد»: صفحة «المنزل الذكي» تربط
  HA (رابط + رمز طويل الأمد، يُتحقَّق قبل الحفظ، الرمز لا يُعرَض) ثم تعرض وتُنفّذ تحديثات كل
  الأجهزة الذكية التي يديرها HA (كيانات `update.*`) من لوحتنا. خدمة + endpoints + جدول إعداد
  + 7 اختبارات. مُختبَر end-to-end ضد HA وهمي (3 تحديثات + تثبيت).
- **ميزة الأمان (CVE)** — خطوة نحو «مكان واحد لكل الأجهزة»: صفحة «الأمان» تعرض لكل جهاز
  مُكتشَف عدد الثغرات المعروفة لمصنّعه وأعلى خطورة ورابط NVD. خدمة `cve.py` تستعلم NVD API
  وتُخزّن مؤقتاً 24 ساعة (تحترم حدود المعدّل، وتخدم من الكاش دون إنترنت) + endpoints
  `/api/security/*` + migration + 5 اختبارات. مُختبَرة حيّاً (TP-Link 494 / Apple 4601).
- أقفال تزامن (HTTP 409) على المسح والفحص والتثبيت لمنع العمليات المتوازية المتضاربة.
- تهيئة مستودع Git مع `.gitignore` جذر شامل.
- **حزمة اختبارات pytest (48 اختباراً)** تحت `backend/tests/` تغطّي: مُحلِّل winget
  (عربي/إنجليزي)، middleware الأمان (Host/CSRF)، upsert المسح مع MAC فارغ، حقن ADB،
  قفل مؤشِّرات التقدُّم، أقفال التزامن، اختبارات smoke، وتشغيل migration. جميعها تجتاز
  على الإصدارات المثبَّتة في `requirements.txt`.
- **Alembic migrations**: بنية كاملة + migration أساسي، و`init_db()` يُشغِّل
  `upgrade head` عند الإقلاع (مع تبنّي قاعدة بيانات قديمة عبر `stamp` بلا انهيار).
- **CI عبر GitHub Actions**: فحص backend (ruff + black + pytest على windows) وبناء
  frontend (tsc + vite على ubuntu). *(يعمل بعد ربط المستودع بـ GitHub ودفعه.)*
- **Phase B — الـ Installer (بداية)**:
  - خدمة الملفّات الساكنة: الـ backend يَخدم الواجهة المبنيّة من "/" (خادم واحد للإنتاج).
  - `launcher.py` + `HomeUpdater.spec`: تجميع PyInstaller ينتج `HomeUpdater.exe` واحداً
    يُقلع ويُهاجر ويَخدم الواجهة والـ API (مُختبَر فعلياً).
  - `installer/HomeUpdater.iss` + `BUILD.md`: سكربت Inno Setup ودليل بناء ثلاثي المراحل.
  - **المثبِّت النهائي مُنتَج ✅**: صُرِّف عبر Inno Setup 6.7 →
    `HomeUpdater-Setup-0.1.0.exe` (~29MB) — ملفّ تثبيت واحد بنقرة مزدوجة يحوي
    الواجهة والـ backend والـ migrations، بالأيقونة وبانرات المعالج.
  - **tray + خدمة (B.6)**: `tray.py` (أيقونة شريط النظام + قائمة، الـ exe صار تطبيق
    tray بلا نافذة console) و`service.py` (خدمة Windows عبر pywin32 للتشغيل headless).
  - **إشعارات Windows (B.6.3)**: toast عبر أيقونة الـ tray عند توفّر تحديثات
    (Windows / تعريفات / برامج)، بلا اعتمادية جديدة، + endpoint `‎/api/system/notify-test`.
- **Phase A — الهوية البصرية (أيقونة)**: علامة افتراضية (منزل + سهم تحديث على خلفية
  زرقاء متدرّجة) عبر `03_الموارد/logo/generate_icons.py`، بكل المقاسات + `.ico`،
  مربوطة بالـ favicon والـ tray وأيقونة الـ exe وأيقونة المثبِّت. قابلة لإعادة التوليد
  من شعار المستخدم لاحقاً.
- **Phase A — بانرات المعالج + splash**: `generate_wizard_banners.py` يُنتج بانرات
  Inno (كبير 164×314 + صغير 55×58، بنسختَي DPI) مع نصّ عربي مُشكَّل صحيحاً، وصورة
  splash 800×800؛ البانرات مربوطة في `HomeUpdater.iss`.

### تغيير (Changed)
- **اكتشاف الشبكة بلغة Python خالصة** (`discovery_python.py`): جسّ TCP + قراءة جدول
  ARP بدل nmap — لا يحتاج nmap/Npcap ولا صلاحيات مدير (يتفادى قيود ترخيص Npcap عند
  التوزيع). nmap يبقى اختيارياً (`scan_method="auto"`). مُختبَر حيّاً: 23 جهازاً في ~5 ثوانٍ.
  إصلاح تعطّل عند MAC مكرَّر في نفس الجولة (جهازان بنفس MAC عبر ARP) — دمجهما بدل الانهيار.
- تنسيق كامل للـ backend بـ black + ruff، وإعداد `pyproject` لتجاهل نمط FastAPI
  (`B008`) واستثناء `alembic/` المُولَّد. إزالة 8 مفاتيح OUI مكرَّرة في
  `mac_vendor.py` كانت تُلبِّس المُصنِّعين (بانتظار تدقيق نهائي مقابل سجلّ IEEE).

### أُزيل / نُظِّف (Removed)
- 5 وحدات ميتة: `services/network.py`, `services/classifier.py`, `models/device.py`,
  حزمة `modules/`, ومكوِّن `components/DevicesTable.tsx`، وملف `system.py` المؤقَّت.
- اعتماديتا واجهة غير مستخدَمتين: `framer-motion`, `react-router-dom`.
- إعادة تنظيم `requirements.txt` إلى ACTIVE مقابل RESERVED (لمراحل قادمة).

## [0.1.0] — 2026-05-08

- Phase 0–1 كاملة: بيئة التطوير، اكتشاف الشبكة (nmap)، قاعدة SQLite، تحديثات Windows،
  تحديثات البرامج (winget) + التعريفات، وإثراء بيانات المُصنِّعين (OUI).
- Phase 2: إدارة هواتف Android عبر ADB over TCP/IP.
- Backend FastAPI + Frontend React/Vite مع ثيمات متعدِّدة وتعدُّد لغات (RTL).
